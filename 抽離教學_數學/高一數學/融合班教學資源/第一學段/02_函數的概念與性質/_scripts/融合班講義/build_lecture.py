# -*- coding: utf-8 -*-
"""講義_函數的概念與定義域_高一數學.docx 產生腳本（2026-08-07 按最新規則重出）。
主題：函數的概念、函數三要素、同一函數判斷、區間記號、求函數定義域。
教學設計：主 D6 明確教學三階段（我做／我們做／你做）＋輔 D2 手順卡（求定義域四步驟）
         ＋輔 D12 自我核對清單（練習端，見 build_practice.py）。
選用理由：「函數」是學生第一次接觸的全新概念（S10），需要完整示範＋放聲思考建立心像；
         「求定義域」是有固定程序的多步驟運算（S2），適合手順卡把步驟外顯化。
"""
import sys, os
sys.stdout.reconfigure(encoding='utf-8')
sys.path.insert(0, r"C:\Users\KongChiLok\.claude\skills\inclusive-math-worksheet-generator\scripts")
from omml_docx import *

SUBJECT = '高一數學'
UNIT = '函數的概念與定義域'
OUT_DIR = os.path.dirname(os.path.abspath(__file__))
ASSETS = os.path.join(OUT_DIR, 'svgs_concept_domain')

P = []
P.append(masthead(SUBJECT, UNIT, '課堂講義'))
P.append(student_info_row())

# ================= 情境引入 =================
P.append(heading('情境：快遞計費'))
P.append(para('某快遞公司按包裹重量 x（公斤）分段收費：重量在 (0,1] 公斤收 15 元；'
              '重量在 (1,3] 公斤收 25 元；重量超過 3 公斤要另外議價，暫不在計費表內。'))
P.append(para('留意到：不論包裹落在哪一段重量範圍，公司都只會給出「唯一一個」收費金額——'
              '這正是函數的核心精神：對於某個範圍（區間）內的每一個 x，都有唯一確定的一個值與它對應。'
              '而「暫不議價」的重量（例如 x=0 或 x>3）就不在這個計費函數的定義域之內。'))

# ================= 一、函數的定義 =================
P.append(heading('一、函數的定義'))
P.append(para('設 A、B 是兩個非空的數集，如果按照某個確定的對應關係 f，使得對於集合 A 中的任意一個數 x，'
              '在集合 B 中都有唯一確定的數 y 和它對應，那麼就稱 f：A→B 為從集合 A 到集合 B 的一個函數，'
              '記作 {y=f(x)}，x∈A。'))
P.append(para('其中：x 叫自變量；x 的取值範圍 A 叫函數的定義域；與 x 對應的 y 值叫函數值；'
              '函數值的集合 ｛{f(x)}｜{x∈A}｝ 叫函數的值域。'))

# ================= 二、函數的三要素與同一函數 =================
P.append(heading('二、函數的三要素'))
P.append(para('函數有三個要素：定義域、對應關係、值域。判斷兩個函數是否為「同一函數」，'
              '要同時檢查：①定義域是否相同；②對應關係是否完全一致——兩項都相同，才是同一函數。'
              '化簡前後式子看起來一樣，不代表就是同一函數，定義域也要對得上。'))

# ================= 三、區間的概念 =================
P.append(heading('三、區間的概念'))
P.append(para('你已經懂得用「｛{x}｜{a≤x≤b}｝」這種集合寫法表示一段數的範圍，區間記號是它的簡寫，'
              '換個寫法會更精簡，數線上的實心點●／空心圈○可以幫助記憶：'))
P.append(shaded_box('閉區間 {[a,b]} = ｛{x}｜{a≤x≤b}｝——兩端點都取，數線上用實心點●表示'))
P.append(shaded_box('開區間 {(a,b)} = ｛{x}｜{a<x<b}｝——兩端點都不取，數線上用空心圈○表示'))
P.append(shaded_box('半開半閉 {[a,b)} = ｛{x}｜{a≤x<b}｝；(a,b] = ｛{x}｜{a<x≤b}｝——一端實心、一端空心'))
P.append(shaded_box('無窮區間：{[a,+∞)}=｛{x}｜{x≥a}｝；{(a,+∞)}=｛{x}｜{x>a}｝；'
                     '(−∞,b]=｛{x}｜{x≤b}｝；{(-∞,b)}=｛{x}｜{x<b}｝'))
P.append(shaded_box('實數集：{R} = {(-∞,+∞)}（正負無窮都取不到，兩端永遠用空心／不寫端點）'))
P.append(image_para(os.path.join(ASSETS, 'interval_demo.png'), width_cm=13.5,
                     caption='圖：實心點●＝端點取得到（closed）；空心圈○＝端點取不到（open）'))

# ================= 四、求函數定義域的四步驟（D2 手順卡） =================
P.append(heading('四、求函數定義域的四步驟'))
P.append(step_card(
    '求函數定義域　手順卡',
    steps=[
        ('看式子裡有沒有根號：被開方數必須「≥0」，把這個條件寫出來。',
         '等於 0 也可以，不是嚴格大於 0。'),
        ('看式子裡有沒有分母：分母必須「≠0」，把這個條件另外寫出來。',
         '只看分母本身，不要把分子一起限制進去。'),
        ('兩個條件都寫出來後，在數線上取「交集」——兩個條件要同時成立的部分。',
         '是交集，不是聯集；只保留兩個條件都滿足的 x。'),
        ('把交集的結果用區間記號寫出來，就是函數的定義域。',
         '端點是實心●還是空心○，要對照原本是「≥／≤」還是「＞／＜／≠」。'),
    ],
    trigger='題目要求「求函數的定義域」，且式子中出現根號或分母時',
    fading='本課（我做／我們做）：手順卡完整呈現，每步都寫出動作與易錯點。'
           '練習B 起：只在區塊開頭重印一次精簡版（步驟數字＋關鍵詞）。'
           '練習C 起：不再重印卡片，改由學生口述四步驟。',
))

# ================= 五、範例：我做／我們做 =================
P.append(heading('五、範例'))

P.append(para('「我做」——老師先示範一次完整的解題過程，並且把心裡想的話說出來：', bold=True))
P.append(para('題目：已知 {f(x)=sqrt(x+5)+frac(1,x-1)}，求 f(x) 的定義域，並求 f(4) 的值。'))
P.append(worked_example_table([
    eq_row('{x+5}', '{0}',
           '「我先看式子裡有什麼：有根號，也有分母，所以有兩個條件要處理。先看根號這邊——'
           '根號裡面的式子不能是負數，所以我要求它 ≥ 0。」', rel='{>=}'),
    eq_row('{x}', '{-5}', '「把 5 移到右邊，範圍就出來了：[-5,+∞)。」', rel='{>=}'),
    span_row('①的解集：{[-5,+∞)}'),
    eq_row('{x-1}', '{0}',
           '「再看分母：分母不可以是 0，所以要求 x−1≠0。」', rel='{!=}'),
    eq_row('{x}', '{1}', '「移項後：x 不能是 1。」', rel='{!=}'),
    span_row('②的解集：{(-∞,1)∪(1,+∞)}'),
    span_row('「兩個條件都要同時成立，所以要取交集——把 x=1 從 [-5,+∞) 挖走。」'),
    span_row('③交集：{[-5,+∞)} ∩ {(-∞,1)∪(1,+∞)} = {[-5,1)∪(1,+∞)}'),
    answer_row('定義域是 {[-5,1)∪(1,+∞)}', why='作答：最後一行用「∴」寫出結論'),
    span_row('「附帶一問：求 f(4)，只要把 x=4 代入原式就可以。」'),
    eq_row('{f(4)}', '{sqrt(4+5)+frac(1,4-1)}', '代入 x=4'),
    eq_row('{f(4)}', '{sqrt(9)+frac(1,3)}', '化簡根號'),
    answer_row('{f(4)=frac(10,3)}', why='作答：最後一行用「∴」寫出結論'),
], why_pct=0.44))

P.append(para('「我們做」——換一題同型的，關鍵決策點留給你填：', bold=True))
P.append(para('題目：已知 {g(x)=sqrt(x+3)+frac(1,x-2)}，求 g(x) 的定義域。'))
P.append(worked_example_table([
    eq_row('{x+3}', '{0}', '這一步要求什麼？想一想再往下寫。', rel='{>=}'),
    eq_row('{x}', '＿＿＿＿', '把 3 移到右邊，範圍是？', rel='{>=}'),
    span_row('①的解集：＿＿＿＿＿＿＿＿＿＿'),
    eq_row('{x-2}', '{0}', '再看分母，這裡要求什麼？', rel='{!=}'),
    eq_row('{x}', '＿＿＿＿', '移項後，x 不能是多少？', rel='{!=}'),
    span_row('②的解集：＿＿＿＿＿＿＿＿＿＿'),
    span_row('把①②的範圍取交集，你的答案是？'),
    span_row('∴ g(x) 的定義域是　＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿＿', bold=True),
], why_pct=0.40))

P.append(para('「你做」——接下來請拿《函數的概念與定義域課堂練習》，用同一套四步驟框架完成練習A、B、C。'))

# ================= 教師實施說明頁 =================
P.extend(teacher_notes(
    main_design='D6 明確教學三階段（我做／我們做／你做）',
    aux_designs=('D2 手順卡（求定義域四步驟）', 'D12 自我核對清單（練習端）'),
    reason='「函數」是學生第一次接觸的全新概念（S10：全新概念導入），沒有先備心像可掛靠，'
           '需要完整示範與放聲思考句建立心像；「求定義域」則是有固定程序的多步驟運算（S2），'
           '適合用手順卡把內隱步驟外顯化。兩個瓶頸依序出現，因此主設計取前端（S10 的 D6），'
           '再疊上 S2 的建議設計（D2）處理後段的程序性技能。',
    density='全班共用',
    fading='D6：本課完整跑「我做→我們做→你做」，之後遇到新的求定義域題型不再重複整套三階段，'
           '直接進入練習層級的鷹架。'
           'D2：本課手順卡完整呈現；練習B 起精簡成區塊開頭一次重印；練習C 起完全不重印，'
           '改由學生口述四步驟。',
    flows=('F5 課前流程預告', 'F3 褪除計劃'),
    iep_codes=('a3 提示題目重點', 'a6 增加行距／放大作答欄'),
))

out = build_docx(P, os.path.join(OUT_DIR, '講義_函數的概念與定義域_高一數學.docx'),
                  footer_text=f'{SUBJECT}．{UNIT}')
print(out)
