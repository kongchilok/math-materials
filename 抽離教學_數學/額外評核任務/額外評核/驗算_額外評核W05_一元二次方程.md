# 驗算紀錄：課堂小測 W05・一元二次方程・概念與三種解法

驗算日期：2026-09-01　驗算人：Claude（主線親自逐題重算，非外包）
交付檔：`小測_初三數學_W05_一元二次方程概念與三種解法_學生卷.docx/.pdf`、同名 `_教師卷.docx/.pdf`
原稿依據：`初三數學\融合班講義練習\01_一元二次方程_概念與三種解法\`（講義／練習／驗算檔）

> 書寫規範：交付檔一律 `x₁ ＝ … 或 x₂ ＝ …`；本表下方 `x=±k` 只是計算稿速記。

## 逐題重算

| 題 | 題型 | 題目 | 卷上答案 | 重算過程 | 一致？ |
|---|---|---|---|---|---|
| 1 | 基礎概念 | `3x²=5x−2` 化一般式後 b＝？ | B（−5） | 移項：`3x²−5x+2=0`；對照 `ax²+bx+c=0` → a＝3、**b＝−5**、c＝2 | ✓ |
| 2 | 基礎概念 | `(x+1)²=4` 的根 | A（`x₁=1` 或 `x₂=−3`） | 右邊 4＞0 可開平方 → `x+1=2` 或 `x+1=−2` → `x₁=1` 或 `x₂=−3` | ✓ |
| 3 | 基礎概念 | `x²−4x+5=0` 的 Δ 與實根個數 | Δ＝−4；0 個實數根 | a＝1、b＝−4、c＝5；`Δ=(−4)²−4(1)(5)=16−20=−4`；Δ＜0 → 無實數根 | ✓ |
| 4 | 常見錯誤糾正 | 小明解 `x²−4=−13` 得 `x=±3` | 錯在由 `x²=−9` 直接開平方；正確為無實數根 | 移項：`x²=−13+4=−9`（小明此步無誤）；−9＜0，負數不能開平方 → 無實數根 | ✓ |
| 5 | 常見錯誤糾正 | 小芳解 `x²=7x` 兩邊除以 x 得 `x=7` | 漏掉 `x=0`；正解 `x₁=0` 或 `x₂=7` | `x²−7x=0` → `x(x−7)=0` → `x=0` 或 `x=7` | ✓ |

## 驗根

| 題 | 代回驗證 |
|---|---|
| 1 | `3x²−5x+2=0`：Δ＝25−24＝1＞0，根為 `x=(5±1)/6` ＝ 1 或 2/3 —— 係數組合合理（此題只考係數，不需解出） |
| 2 | `x₁=1`：`(1+1)²=4` ✓；`x₂=−3`：`(−3+1)²=(−2)²=4` ✓ |
| 3 | Δ＜0 無實根 —— 另驗：`x²−4x+5=(x−2)²+1 ≥ 1 > 0`，恆正無零點 ✓ |
| 4 | `x²=−9`：任何實數平方 ≥ 0，故無實數解 ✓ |
| 5 | `x₁=0`：左 `0²=0`，右 `7(0)=0` ✓；`x₂=7`：左 `7²=49`，右 `7(7)=49` ✓ |

## 誘答選項驗算（第 1、2 題）

| 題 | 誘答 | 對應的學生錯誤 | 該錯誤會算出什麼 | 一致？ |
|---|---|---|---|---|
| 1 | A（5） | 移項時漏變號，把 `5x` 直接抄成 `+5x` | 誤得 `3x²+5x+2=0` → b＝5 | ✓ |
| 1 | C（3） | 錯把 a 當 b | a＝3 | ✓ |
| 1 | D（2） | 錯把 c 當 b | c＝2 | ✓ |
| 2 | B（`x₁=3` 或 `x₂=−5`） | 誤把右邊當 16 開平方 | `x+1=±4` → 3 或 −5 | ✓ |
| 2 | C（`x₁=2` 或 `x₂=−2`） | 開平方後忘記移項（講義手順卡①第 4 步） | 停在 `x+1=±2` 直接答 ±2 | ✓ |
| 2 | D（無實數根） | 誤判右邊正負 | 右邊 4＞0，實際有兩根 | ✓（純干擾項） |

## 錯誤原型溯源（第 4、5 題）

| 題 | 原型出處 | 原題 | 本卷改動 |
|---|---|---|---|
| 4 | 練習第 6 題 | 小華解 `x²=−9` 寫成 `x=±3` | 加一步移項（`x²−4=−13`），令學生要先自行整理再判斷正負；錯誤點不變 |
| 5 | 練習第 23 題 ＋ 其註記「兩邊都有 x 時不可以除以 x」 | `x²=5x` | 數據改為 `x²=7x`，並把「除以 x」寫成學生算式讓學生指認，由解題改為糾錯 |

## 結構覆蓋檢查

| 講義章節 | 本卷是否覆蓋 |
|---|---|
| 一、一元二次方程與一般式 | ✓ 第 1 題 |
| 二、方法一 直接開平方法 | ✓ 第 2 題（正例）、第 4 題（負數反例） |
| 三、方法二 配方法 | ✗ **未覆蓋**（已在教師卷「三、命題說明」標明，留待下次小測） |
| 四、方法三 公式法（判別式） | ✓ 第 3 題 |
| 五、方法四 因式分解法 | △ 第 5 題用提公因式分解，未考十字交乘型 |

## 作答時間估算（5 分鐘可行性）

| 題 | 動作 | 估時 |
|---|---|---|
| 1 | 移項＋讀係數＋圈選項 | ~25 秒 |
| 2 | 開平方分兩支＋移項＋圈選項 | ~35 秒 |
| 3 | 代判別式公式計算＋判斷 | ~50 秒 |
| 4 | 指認錯步＋寫正確結論（約 20 字） | ~80 秒 |
| 5 | 指認漏根＋寫完整解（4 行算式） | ~90 秒 |
| | **合計** | **約 4 分 40 秒** |

> 上限 5 分鐘，餘裕約 20 秒。填姓名班別學號另計（建議上課前先發卷讓學生填好再計時）。

## 結論

5 題全部逐題重算並代回驗根，**與卷上答案完全一致，無數學錯誤**。
誘答選項 6 個全部對應真實學生錯誤且計算一致（無「怎樣算都算不出來」的空誘答）。
兩道糾錯題的錯誤原型均可溯源至本單元現成教材，非憑空杜撰。
