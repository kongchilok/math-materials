# 驗算紀錄：額外評核 W13・配方求頂點、逆向讀圖與待定係數法

驗算日期：2026-09-20　驗算人：Claude（主線親自逐題重算，非外包）
交付檔：`額外評核_初三數學_W13_配方求頂點與待定係數法_學生卷.docx/.pdf`、同名 `_教師卷.docx/.pdf`
產生腳本：`build_quiz_w13.py`
原稿依據：`初三數學\融合班講義練習\05_二次函數的圖像與性質\`

## 逐題重算

| 題 | 題型 | 題目 | 卷上答案 | 重算過程 | 一致？ |
|---|---|---|---|---|---|
| 1 | 基礎（配方） | `y=x²−8x+11` 化頂點式 | B `y=(x−4)²−5` | `x²−8x+16−16+11=(x−4)²−5` | ✓ |
| 2 | 基礎（五樣齊出三樣） | `y=x²+4x−5` 的頂點式、頂點、y 軸交點 | `(x+2)²−9`；`(−2,−9)`；`(0,−5)` | 配方 `x²+4x+4−4−5=(x+2)²−9`；`h=−2`、`k=−9`；`x=0` → `y=−5` | ✓ |
| 3 | 基礎（逆向讀圖） | 與 x 軸交於 `(−1,0)`、`(5,0)`，頂點縱坐標 −9 | 軸 `x=2`；頂點 `(2,−9)` | 對稱軸在兩交點正中：`(−1+5)/2=2`；頂點在軸上 → `(2,−9)` | ✓ |
| 4 | 常見錯誤糾正 | 小明把 `y=x²−10x+18` 配成 `(x−5)²+18` | 漏減回 25；正確 `(x−5)²−7`、頂點 `(5,−7)` | `x²−10x+25−25+18=(x−5)²−7` | ✓ |
| 5 | 常見錯誤糾正 | 小芳設 `y=a(x+1)²+2`（頂點 `(1,2)`） | h 符號錯；正確 `y=2(x−1)²+2` | 頂點 `(1,2)` → `h=1`、`k=2` → 設 `y=a(x−1)²+2`；代 `(3,10)`：`4a+2=10` → `a=2` | ✓ |

## 展開回原式核對（配方題的標準驗法）

| 題 | 展開驗證 |
|---|---|
| 1 | `(x−4)²−5=x²−8x+16−5=x²−8x+11` ✓ 與原式相同 |
| 2 | `(x+2)²−9=x²+4x+4−9=x²+4x−5` ✓；另代 `x=0`：頂點式 `(0+2)²−9=−5`，原式 `−5` ✓ 兩式一致 |
| 3 | 由兩根反推：`y=a(x+1)(x−5)`；頂點在 `x=2` 時 `y=a(3)(−3)=−9a`，已知 `=−9` → `a=1`；故 `y=(x+1)(x−5)=x²−4x−5=(x−2)²−9` ✓ 與答案的軸 `x=2`、頂點 `(2,−9)` 相符 |
| 4 | `(x−5)²−7=x²−10x+25−7=x²−10x+18` ✓；小明的 `(x−5)²+18=x²−10x+43≠` 原式 ✗ 可直接否證 |
| 5 | `y=2(x−1)²+2`：頂點 `(1,2)` ✓；代 `x=3`：`2(2)²+2=10` ✓ 通過已知點 |

## 誘答選項驗算（第 1 題）

| 誘答 | 對應的學生錯誤 | 展開後得什麼 | 一致？ |
|---|---|---|---|
| A `(x−4)²+11` | 補了 `+16` 卻沒有減回 16（最高頻，＝第 4 題原型） | `x²−8x+16+11=x²−8x+27` ≠ 原式 | ✓ |
| C `(x−8)²−53` | h 取成 b 而不是 `b/2` | `x²−16x+64−53=x²−16x+11` ≠ 原式 | ✓ |
| D `(x+4)²−5` | h 的符號取反 | `x²+8x+16−5=x²+8x+11` ≠ 原式 | ✓ |

## 錯誤原型溯源（第 4、5 題）

| 題 | 原型出處 | 原題 | 本卷改動 |
|---|---|---|---|
| 4 | 練習 C 第 9 題（找錯題：`y=x²−4x+3` 誤配成 `(x−2)²+3`，正確為 `(x−2)²−1`） | 同結構 | 數據改為 `y=x²−10x+18`，補回值由 4 改為 25（數字較大，更易看出「補了就要減」的必要） |
| 5 | 練習 B 第 6 題、練習 C 第 8 題（待定係數法設元環節） | 設頂點式代點求 a | 自擬頂點 `(1,2)` 過 `(3,10)`；由「求解析式」改寫成「指出設元錯再改正」 |

## 結構覆蓋檢查

| 講義章節 | 本卷是否覆蓋 |
|---|---|
| §五 配方（一般式化頂點式） | ✓ 第 1、2、4 題 |
| 五樣齊出（開口→軸→頂點→x 軸交點→y 軸交點） | △ 只考頂點式、頂點、y 軸交點三樣（5 分鐘容量所限） |
| 逆向讀圖（由交點反推對稱軸與頂點） | ✓ 第 3 題 |
| §六 待定係數法（頂點式） | ✓ 第 5 題 |
| 待定係數法（一般式、交點式） | ✗ 乙班只教頂點式 → 剔除 |
| 圖像法解一元二次不等式 | ✗ 乙班全學段無此節 → 剔除 |

## 🔴 進度覆蓋與兩班交集核對

| 內容 | 丙班（進度基準） | 乙班 | 本卷是否考 |
|---|---|---|---|
| 配方求頂點 | 11/13 | 11/16 | ✓（兩班均在 W12 或之前教完） |
| 逆向讀圖 | 11/17 | 11/16「五樣齊出」、11/12 讀頂點 | ✓ |
| 待定係數法（頂點式） | 11/18（三式之一） | 11/18（只做頂點式） | ✓ 取交集 |
| 待定係數法（一般式／交點式） | 11/18 | **無** | ✗ 剔除 |
| **圖像法解一元二次不等式** | 11/20 已教 | **全學段無此節**（乙班 11/19 上的是「二次函數與一元二次方程的關係」） | ✗ **剔除** |

## 作答時間估算

| 題 | 動作 | 估時 |
|---|---|---|
| 1 | 配方＋比對選項 | ~50 秒 |
| 2 | 配方＋讀頂點＋代 x=0（三格） | ~85 秒 |
| 3 | 取兩交點中點＋組合頂點（兩格） | ~40 秒 |
| 4 | 指認漏減回＋重配方＋寫頂點 | ~65 秒 |
| 5 | 指認 h 符號錯＋重設＋代點求 a | ~75 秒 |
| | **合計** | **約 5 分 15 秒** |

> ⚠ 本卷估時**超出 5 分鐘上限約 15 秒**，是全學段最緊的一張。
> 已列為交付後待決事項：若實施時多數學生做不完，建議把第 2 題由三格減為兩格
> （刪去「與 y 軸的交點」一格，預計可省約 25 秒），或把第 3 題刪去。
> 本輪不擅自改動，因為「5 分鐘可行性壓力測試」在規格卡屬排除項，
> 是否調整應由使用者按首次實施的實況決定。

## 結論

5 題全部逐題重算，所有配方結果一律**展開回原式**核對，
待定係數法結果一律代回已知點與頂點核對，
**與卷上答案完全一致，無數學錯誤**。
第 1 題三個誘答已逐一展開，確認三者展開後都不等於原式，且各自對應一種真實錯誤。
🔴 兩項範圍剔除（圖像法解不等式、待定係數法的一般式與交點式）已逐節核對乙班教案確認。
⚠ 估時超標 15 秒，已如實記錄並列入待決事項，未自行修改題目。
