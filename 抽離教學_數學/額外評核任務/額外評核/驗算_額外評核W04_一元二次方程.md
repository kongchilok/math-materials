# 驗算紀錄：額外評核 W04・一元二次方程・三種解法鞏固

驗算日期：2026-09-18　驗算人：Claude（主線親自逐題重算，非外包）
交付檔：`額外評核_初三數學_W04_一元二次方程三種解法鞏固_學生卷.docx/.pdf`、同名 `_教師卷.docx/.pdf`
產生腳本：`build_quiz_w04.py`
規格依據：`build_quiz_w05.py` 檔頭規格卡（5 分鐘／5 題／3 基礎＋2 糾錯／每題 20%）

> 書寫規範：交付檔一律 `x₁ ＝ … 或 x₂ ＝ …`；本表下方 `x=(5±3)/4` 只是計算稿速記。

## 一、週次與命題範圍的獨立核對

週次**不採信舊檔 W05 的編法**，改按基準檔獨立核對（記憶 `verify_upstream_dates_not_just_consistency`）：

| 來源 | 內容 | 結論 |
|---|---|---|
| `抽離教學_數學\校曆_26-27_上課日曆.md` §三 | W1＝08/31–09/04、W2＝09/07–09/11、W3＝09/14–09/18、**W4＝09/21–09/25** | 下週＝**W04** |
| `初三數學\教學計劃_26-27初三抽離_乙丙班.md` | 同一套 W 編號（W1–W44），並註明「所有週次皆源自校曆檔，勿各自另算」 | 兩檔一致 |

⚠️ **既有 `W05` 卷的編號與校曆對不上**（該卷驗算日期 2026-09-01＝W1，內容卻涵蓋 09/15 才教的判別式）。
本次未改動該檔，留待使用者裁決其正確週次。

命題範圍＝上週（W3，09/14–09/18）已教完的內容，按兩班教案逐日核對：

| 班 | W3 實際內容 | 出處 |
|---|---|---|
| 丙（進度基準） | 09/15 方法三（一）公式法引入、09/16 方法三（二）應用、09/18 方法四（二）因式分解應用＋常見錯誤辨析 | `抽離課堂教學教案_初三丙班_全學年.md` |
| 乙 | 09/14 方法三（一）、09/16 方法三（二）、09/17 方法四（一）因式分解捷徑 | `抽離課堂教學教案_初三乙班_全學年.md` |

兩班在 W3 結束前均已教完配方法（丙 09/09、09/11；乙 09/09）、公式法、因式分解法，本卷 5 題全部在範圍內。
唯一時序注意：第 5 題的「移項令右邊為 0」乙班排在 **09/21（週一）**，故教師卷已註明本卷宜在 09/21 之後實施。

補回項：W05 卷教師頁自行標明「本次未覆蓋配方法與因式分解法（十字交乘型），可留待下一次小測」——本卷第 1、3 題正是補回這兩項。

## 二、逐題重算

| 題 | 題型 | 題目 | 卷上答案 | 重算過程 | 一致？ |
|---|---|---|---|---|---|
| 1 | 基礎概念 | 用配方法解 `x²−8x+3=0`，兩邊應同加？ | C（16） | 移項 `x²−8x=−3`；一次項係數 −8 的一半為 −4，`(−4)²=16` | ✓ |
| 2 | 基礎概念 | 用公式法解 `2x²−5x+2=0` | A（`x₁=2` 或 `x₂=1/2`） | a＝2、b＝−5、c＝2；`Δ=(−5)²−4(2)(2)=25−16=9`；`x=(5±3)/4` → 2 或 1/2 | ✓ |
| 3 | 基礎概念 | 用因式分解法解 `x²−5x+6=0` | `x₁=2` 或 `x₂=3` | 積 6、和 −5 → −2 與 −3；`(x−2)(x−3)=0` | ✓ |
| 4 | 常見錯誤糾正 | 小強算 `Δ=(−3)²−4(1)(−4)=9−16=−7` | 錯在 `−4ac` 的符號；`Δ=25`，`x₁=4` 或 `x₂=−1` | c＝−4，`−4(1)(−4)=+16`；`Δ=9+16=25>0`；`x=(3±5)/2` → 4 或 −1 | ✓ |
| 5 | 常見錯誤糾正 | 小玲由 `(x−1)(x+2)=4` 推出 `x−1=4` 或 `x+2=4` | 錯在右邊不是 0 就拆因式；`x₁=−3` 或 `x₂=2` | 展開 `x²+x−2=4` → `x²+x−6=0` → `(x+3)(x−2)=0` | ✓ |

## 三、驗根

| 題 | 代回驗證 |
|---|---|
| 1 | `(x−4)²=13`：展開 `x²−8x+16=13` → `x²−8x+3=0` ✓ 與原方程相同 |
| 2 | `x₁=2`：`2(4)−5(2)+2=8−10+2=0` ✓；`x₂=1/2`：`2(1/4)−5(1/2)+2=0.5−2.5+2=0` ✓ |
| 3 | `x₁=2`：`4−10+6=0` ✓；`x₂=3`：`9−15+6=0` ✓。另驗韋達：和 5＝−b/a ✓、積 6＝c/a ✓ |
| 4 | `x₁=4`：`16−12−4=0` ✓；`x₂=−1`：`1+3−4=0` ✓ |
| 5 | `x₁=−3`：`(−3−1)(−3+2)=(−4)(−1)=4` ✓；`x₂=2`：`(2−1)(2+2)=(1)(4)=4` ✓ |

## 四、誘答選項驗算（第 1、2 題）

| 題 | 誘答 | 對應的學生錯誤 | 該錯誤會算出什麼 | 一致？ |
|---|---|---|---|---|
| 1 | A（4） | 只做了「取一半」，忘記再平方 | `(−8)÷2=−4` → 取數值 4 | ✓ |
| 1 | B（8） | 直接抄一次項係數的絕對值 | 8 | ✓ |
| 1 | D（64） | 先平方後才除以 2 的次序倒轉／直接平方係數 | `(−8)²=64` | ✓ |
| 2 | B（4 或 1） | 分母寫成 2 而不是 `2a=4` | `(5±3)/2` → 4 或 1 | ✓ |
| 2 | C（7/2 或 −1） | 忘記把 Δ 開平方，直接用 9 代入 | `(5±9)/4` → 14/4＝7/2、−4/4＝−1 | ✓ |
| 2 | D（無實數根） | 誤把 `Δ=9` 當成負數／誤算 `25−16` | Δ＝9＞0，實際有兩根 | ✓（純干擾項） |

6 個誘答全部對應真實學生錯誤且計算一致，無「怎樣算都算不出來」的空誘答。

## 五、錯誤原型溯源（第 4、5 題）

| 題 | 原型出處 | 本卷改動 |
|---|---|---|
| 4 | 公式法「手順卡③」代入步驟的指定易錯點：`−4ac` 當 c＜0 時的正負號 | 自行命題；選 `x²−3x−4=0` 令錯誤結論恰好是「無實數根」，與正確的「兩個不等實根」對比最強烈 |
| 5 | 丙班 09/18／乙班 09/21 課堂總結欄原文「覆核移項使右邊為 0 這一步不可漏」 | 自行命題；把錯誤寫成學生算式讓學生指認，由解題改為糾錯（沿用 W05 卷第 4、5 題的做法） |

## 六、與 W05 卷的重複檢查

| W05 考點 | 本卷是否重複 |
|---|---|
| 化一般式、讀係數 | ✗ 不重複 |
| 直接開平方法 | ✗ 不重複 |
| 判別式 Δ 的計算與根數判斷 | △ 第 2、4 題要算 Δ，但落點在「求根公式的完整代入」與「符號錯誤」，非判根數 |
| 負數不能開平方 | ✗ 不重複 |
| 兩邊除以 x 漏根 | ✗ 不重複（第 5 題是另一種因式分解錯誤：右邊不為 0） |

## 七、作答時間估算（5 分鐘可行性）

| 題 | 動作 | 估時 |
|---|---|---|
| 1 | 移項＋取一半＋平方＋圈選項 | ~30 秒 |
| 2 | 算 Δ＋代公式分兩支＋對選項 | ~55 秒 |
| 3 | 十字交乘＋分兩支寫解 | ~55 秒 |
| 4 | 指認錯步＋重算 Δ＋寫兩個根 | ~85 秒 |
| 5 | 指認錯處＋展開移項＋分解＋寫兩個根 | ~90 秒 |
| | **合計** | **約 5 分 15 秒** |

⚠️ 略為超出 5 分鐘上限（W05 卷為 4 分 40 秒）。原因：本卷 3 題需要完整解方程，
W05 卷只有 2 題。**實施建議**（擇一）：
1. 上課前先發卷讓學生填好姓名班別學號，計時只計作答；
2. 或將第 3 題改為「只寫出分解式」（教師卷評分欄現要求完整解，需同步改口）；
3. 或接受 5 分半鐘。
建議採第 1 項（W05 卷已有同樣備註），不改題。

## 八、排版檢查與修正紀錄（目視 PDF 逐頁核）

| # | 發現 | 處理 |
|---|---|---|
| 1 | 學生卷溢出到第 2 頁 | 收框內上下內邊距 40→20 twips、題框間隔段 line 120→60、書寫線段前距 60→20；第 4 題題幹由「他錯在哪一步？正確答案是？」縮為「他錯在哪？正確答案？」令題幹收回一行。字級與框線一律不動（記憶 `table_doc_page_saving_order`） |
| 2 | 第 3–5 題第二條作答線看不見 | 相鄰段落框線設定相同會被 Word 合併（記憶 `docx_adjacent_border_paras_merge`）。`ruled()` 加 `alt` 參數，逐條交替 `w:space` 並加 1 twip 右縮排；現 15 條書寫線全部可見 |
| 3 | 教師卷「命題說明」的 `{−4ac}` 印出大括號 | 該段用 `para([('t', …)])` 純文字型態，不會解析 `{}` 標記；改寫為純文字 `−4ac` |
| 4 | 教師卷第 5 題一行內有三個等號 | 拆成三行，一行一個等號（house-style 書寫規範第 1 條） |

⚠️ 同樣的第 2 項缺陷仍存在於 `build_quiz_w05.py`（該檔自有一份 `ruled()`，未修）。

最終版面：學生卷 1 頁、教師卷 3 頁。`qa_check.py` 全資料夾 63 項通過；
9 項 QB-6 失敗全部為「檔名前綴不在白名單」——新分類名（課後補救教學／抽離課堂檢測／額外評核）
尚未加入共用 skill 的 `GENRE_RE`，未擅自改動共用腳本，留待使用者裁決。

## 九、結論

5 題全部逐題重算並代回驗根，**與卷上答案完全一致，無數學錯誤**。
誘答 6 個全部可溯源至真實學生錯誤。兩道糾錯題的錯誤原型均來自本單元課堂指定易錯點，非憑空杜撰。
週次 W04 已獨立對照校曆基準檔核實，未沿用舊檔 W05 的編法。
唯一待決事項：作答時間估算 5 分 15 秒，略超上限，見 §七。
