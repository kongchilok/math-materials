# 驗算紀錄：額外評核 W09・無理方程與二元二次方程組

驗算日期：2026-09-20　驗算人：Claude（主線親自逐題重算，非外包）
交付檔：`額外評核_初三數學_W09_無理方程與二元二次方程組_學生卷.docx/.pdf`、同名 `_教師卷.docx/.pdf`
產生腳本：`build_quiz_w09.py`
原稿依據：`初三數學\融合班講義練習\04_分式方程無理方程與二元二次方程組\`

## 逐題重算

| 題 | 題型 | 題目 | 卷上答案 | 重算過程 | 一致？ |
|---|---|---|---|---|---|
| 1 | 基礎（快速判準） | `√(2x+1)=−3` 解的情況 | C（無解） | 根號的結果必為非負，左邊 ≥0；右邊 −3<0 → 左右不可能相等 → 無解 | ✓ |
| 2 | 基礎（代入＋回代） | 解 `y=x−2`；`x²+y²=10` | `(3,1)` 或 `(−1,−3)` | 代入：`x²+(x−2)²=10` → `2x²−4x−6=0` → `x²−2x−3=0` → `(x−3)(x+1)=0` → `x₁=3` 或 `x₂=−1`；回代 `y=x−2` → `y₁=1`、`y₂=−3` | ✓ |
| 3 | 基礎（單考回代） | 已得 `x₁=2`、`x₂=−5`，另一式 `y=3−x` | `(2,1)` 或 `(−5,8)` | `y₁=3−2=1`；`y₂=3−(−5)=3+5=8` | ✓ |
| 4 | 常見錯誤糾正 | 小明只答 x 不答 y | 漏回代；`(2,5)` 或 `(−5,−2)` | `x²+(x+3)²=29` → `2x²+6x−20=0` → `x²+3x−10=0` → `(x+5)(x−2)=0` → `x₁=2` 或 `x₂=−5`；回代 `y=x+3` → `y₁=5`、`y₂=−2` | ✓ |
| 5 | 常見錯誤糾正 | 小芳把 `(x+2)²` 展成 `x²+4` | 漏中間項 `4x`；正確 `x=1` | `(x+2)²=x²+4x+4`；`2x+7=x²+4x+4` → `x²+2x−3=0` → `(x+3)(x−1)=0` → `x₁=1` 或 `x₂=−3`；驗根捨 −3 | ✓ |

## 驗根／代回原方程（組）

| 題 | 代回驗證 |
|---|---|
| 1 | 若硬平方：`2x+1=9` → `x=4`；代回原方程左 `=√9=3`，右 `=−3`，`3≠−3` ✗ → 確認無解（平方法會生出假根，正是本題要學生避開的）|
| 2 | `(3,1)`：`1=3−2` ✓；`9+1=10` ✓｜`(−1,−3)`：`−3=−1−2` ✓；`1+9=10` ✓ |
| 3 | `(2,1)`：`1=3−2` ✓｜`(−5,8)`：`8=3−(−5)=8` ✓ |
| 4 | `(2,5)`：`5=2+3` ✓；`4+25=29` ✓｜`(−5,−2)`：`−2=−5+3` ✓；`25+4=29` ✓ |
| 5 | `x=1`：左 `=√(2+7)=√9=3`，右 `=1+2=3` ✓ **保留**｜`x=−3`：左 `=√(−6+7)=√1=1`，右 `=−3+2=−1`，`1≠−1` ✗ **捨去** |

## 誘答選項驗算（第 1 題）

| 誘答 | 對應的學生錯誤 | 該錯誤會算出什麼 | 一致？ |
|---|---|---|---|
| A（一個解） | 一律先平方再解，解出 `x=4` 就交卷，不驗根 | 得 `x=4`，但代回不成立 | ✓ |
| B（兩個解） | 誤以為有根號就一定有兩支（把 `√` 與「開平方 ±」混淆） | `2x+1=9` 是一次方程，本來就只得一解 | ✓ |
| D（無法判斷） | 純干擾項：以為不解就判不到 | — | ✓（無計算路徑，純概念干擾） |

## 錯誤原型溯源（第 4、5 題）

| 題 | 原型出處 | 本卷改動 |
|---|---|---|
| 4 | 講義範例三的 QA 紀錄明載「舊版此題省略了『回代求 y』一步（subagent 驗出），本版補回並獨立成行」；丙班教案 10/23 總結欄口訣「有幾個 x 就要交幾對」 | 把該漏步直接寫成學生的錯誤答案，數據取 `y=x+3`、`x²+y²=29`（練習 A3 為 `y=x−1`、`x²+y²=13`，已更換） |
| 5 | 練習 A2（`√(x+3)=x−3`）平方那一步的高頻錯：完全平方漏中間項 | 數據改為 `√(2x+7)=x+2`，並令改正後剛好有一支要捨，順帶再考一次驗根 |

## 結構覆蓋檢查

| 講義章節 | 本卷是否覆蓋 |
|---|---|
| 無理方程：快速判準（根號結果非負） | ✓ 第 1 題 |
| 無理方程：平方展開＋驗根 | ✓ 第 5 題 |
| 二元二次方程組：代入消元 | ✓ 第 2、4 題 |
| 二元二次方程組：回代求另一未知數 | ✓ 第 2、3、4 題（第 3 題單獨抽出） |
| 分式方程 | ✗ 已於 W08 考過 |

## 進度覆蓋與兩班交集核對

| 內容 | 丙班（進度基準） | 乙班 | 本卷是否考 |
|---|---|---|---|
| 無理方程（含快速判準） | 10/16、10/20、10/21 | 10/15 | ✓ 第 1、5 題 |
| 二元二次方程組：代入消元 | （10/23 前已教，教案該節被 v1.2 移除週四後併入） | 10/21 | ✓ |
| 二元二次方程組：回代 | 10/23 | 10/22 | ✓ |

> 註：第 1 題的「根號結果不會是負數」判準，丙班 10/20 在總結欄明文補充過；
> 乙班雖無獨立一節，但該判準正是兩班共用講義範例二捨根的直接理由
> （`√(−1+2)=1≠−1`），屬已教內容的直接推論，可用。

## 作答時間估算

| 題 | 動作 | 估時 |
|---|---|---|
| 1 | 看右邊正負＋圈選項 | ~20 秒 |
| 2 | 代入、展開、整理、解、回代、寫兩對 | ~90 秒 |
| 3 | 兩次回代（其中一次含負負得正） | ~40 秒 |
| 4 | 指認漏步＋兩次回代＋寫兩對 | ~60 秒 |
| 5 | 指認展開錯＋重解＋驗根＋答 | ~80 秒 |
| | **合計** | **約 4 分 50 秒** |

> 上限 5 分鐘，餘裕約 10 秒。第 2 題是全卷最長的一題，若實施偏緊，
> 下次可把第 2 題的二次方程係數改為更易分解者，或把第 3 題刪去。

## 結論

5 題全部逐題重算，方程組的解一律代回**原方程組兩條方程**核對，
無理方程的根一律代回**原方程**（不是平方後的方程）核對，
**與卷上答案完全一致，無數學錯誤**。
第 1 題三個誘答中兩個對應真實學生錯誤並已實算確認會導向錯誤結論。
第 2 題刻意取「兩支解都合理」，與 W07 第 3 題呼應，
持續打破學生「一定要捨一支」的慣性。
兩道糾錯題的錯誤原型分別取自講義範例三的 QA 修訂紀錄與練習 A2，皆為現成教材。
進度已逐節核對乙、丙兩班教案，本卷無落差。
