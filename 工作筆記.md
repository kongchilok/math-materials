# 📋 notebookLM 工作筆記

## 📋 需求確認

### 🟢 現行規格卡：初三數學抽離課堂「每週 5 分鐘小測」
- **規格狀態**：`confirmed` ✅（2026-09-01，經 RDQ-Full 訪談）｜`revisions: 1`｜`updated_at: 2026-09-01`
  - revisions 1（同日執行後修正）：排除項第 4 條原寫「QA 會靜靜 skip」，**實測是直接 FAIL**，並查明加白名單亦不足以解決（見下）。
- **一段式需求**：為初三抽離小班（乙／丙班）制定每週 1 次、課內限時 5 分鐘的 5 題小測；
  本輪只出 **1 次樣板**（學生卷＋教師答案卷，docx＋PDF），用 `similar-practice-generator`
  依現成單元教材出相似題，存 `抽離教學_數學\額外評核任務\`。
- **硬限制**：5 分鐘／5 題／每題 20%／單次滿分 5 分；命題範圍＝**上週已教完**（隔一週鞏固）；
  進度基準＝**丙班**；兩班共用同一份卷。
- **學段結算**：個人累積正確率（含改正後）× 20 折算為平時平均分加分，**上限 20 分**。
- **樣板定於 W5（2026/09/28–10/02），範圍＝單元 01「一元二次方程・概念與三種解法」**
  （教學計劃 §4.2／§4.3：乙班 W3(2)+W4(2)、丙班 W3(3)+W4(2)，**兩班都在 W4 教完，零進度落差**）。
- **產出 5 個檔**：`小測_初三數學_W05_一元二次方程概念與三種解法_學生卷.docx/.pdf`、
  同名 `_教師卷.docx/.pdf`、`驗算_小測W05_一元二次方程.md`。
- **❓ 假設**：①3 題基礎概念＋2 題常見錯誤糾正 ②「常見錯誤」取自單元 01 現成教材與驗算檔的實際易錯點
  ③加分＝個人累積正確率×20（上限 20）④「含改正後」＝改正正確者該題計全額正確
  ⑤檔名前綴 `小測_`、masthead 類型「課堂小測」（不用 skill 預設的 `功課_`／「課後練習」）
  ⑥學生卷不印答案與計分規則細節，改正欄只留空格。
- **⚠️ 風險**：跟丙班進度出卷，丙班全學年多 15 節，兩班進度會愈拉愈開；樣板週 W5 無影響，
  但擴到整個學段時約 W9 之後乙班有機會測到未教內容，擴產前要再決定處理方式。
- **❌ 排除項（下游技能不得自行加回）**：5 分鐘可行性壓力測試｜改正折算率寫死在卷面｜
  融合生限時調適（延長至 7 分鐘／減題）｜`qa_check.py` 的 `GENRE_RE` 補「小測」前綴
  （實測後果見下面 2026-09-01 第二輪紀錄）｜學段記分表｜乙班未教題標示｜上傳 NotebookLM｜第 2 次之後的小測。

### ⚪ 已結案規格卡（2026-08-05 立、2026-08-15 結）
- **規格狀態**：`confirmed` ✅（2026-08-05）
- **規格卡**：融合班講義的**數學書寫規範**改版
  - 目標：範例段等號上下對齊；開平方／因式分解／拆 ± 後分兩支寫並寫「或」；最後一行用「∴」寫答句
  - 對象：初三融合班（抽離小班）
  - 產出格式：docx＋PDF 雙格式，規範同步寫回 house-style.md
- **遵行原則**：`±` 只保留在印出來的公式本身（求根公式、通式），解題步驟一律拆兩支 ✅ **已確認、已內化到 skill**
- **狀態**：✅ 已於 2026-08-15 全部結案（18 個初三單元全部完成／判定不適用，見下面同日紀錄）；
  練習答案區使用者裁決不套用

---

## ⏯️ 上次做到哪
**2026-09-01（第二輪）：用 RDQ-Full 訪談定出「初三每週 5 分鐘小測」需求規格卡並確認，隨即出 W05 樣板一套（學生卷 1 頁／教師卷 2 頁，docx＋PDF）＋驗算檔。QA 14 PASS／2 FAIL，兩個 FAIL 同源，且查出「加白名單」解決唔到。**
- **RDQ-Full 訪談**（打擾使用者 3 次，符合 Full 預算）：份量＝先出 1 次樣板；班別＝兩班共用一份、**跟丙班進度**；命題範圍＝**上週已教完**（隔一週鞏固）；卷面＝學生卷（含改正欄）＋教師答案卷。象限Ⅳ 四項建議使用者全部不勾 → 全部記入 ❌ 排除項。規格卡已寫入本檔「📋 需求確認」，`status: confirmed`。
- **樣板週次由我按教學計劃推定並寫明理由**：選 **W5**，因為 §4.2／§4.3 顯示乙班 W3(2)+W4(2)、丙班 W3(3)+W4(2)，**兩班都在 W4 教完單元 01**，零進度落差——樣板不受「跟丙班進度」那個風險影響。
- **產出**（`抽離教學_數學\額外評核任務\`，新資料夾）：`小測_初三數學_W05_一元二次方程概念與三種解法_學生卷/.教師卷` 各 docx＋PDF、`驗算_小測W05_一元二次方程.md`、建置腳本 `build_quiz_w05.py`。
- **5 題**：1 化一般式讀係數｜2 直接開平方｜3 判別式判根數｜4 糾錯：負數開平方｜5 糾錯：兩邊除以 x 漏根。第 4、5 題錯誤原型溯源自本單元《課堂練習》第 6、23 題，數據已更換。逐題估作答時間合計 **約 4 分 40 秒**（限時 5 分鐘）。
- **過程修好 5 個缺陷**（每輪都出 PDF 目視＋座標實測）：
  1. 🔴 **「每題 20 分」自相矛盾**（單次滿分只有 5 分）→ 改「每題 1 分（20%）」。
  2. 🔴 學生卷溢出 2 頁 → 收框內距（80→40 twips）＋以矮身段取代 `problem_box` 的 `trailing_blank` 空段，收回 **1 頁**；**字級／行距／邊界一律未動**（house-style 那三項要問過使用者才可改）。
  3. 🔴 教師卷 3 頁、第 2 頁只得 3 行 → 移除硬分頁，收成平衡的 2 頁。
  4. 🔴 **第 4、5 題的提問文字佔用了作答線本身**，學生實際只剩 1 條線寫約 40 字 → 提問併入題幹段，兩條線全部留空。純抽字驗證揪唔到呢個，係目視先發現。
  5. 彩色 ⚠️ emoji（黑白列印會變灰塊）→ 改「※」。
- **QA：16 項、14 PASS、2 FAIL**，兩個 FAIL 都係 **QB-6 檔名慣例**（「小測」不在 `GENRE_RE`）。已人手補驗被跳過的兩項：QB-8 學生資訊列 ✅、QB-10 題答對應 ✅。
- **本輪未 commit 前一輪嗰批含真名的檔**（丙班／補救班／高一戊班教案），私隱阻塞仍然未解，等使用者裁決。

---

**2026-09-01（第一輪）：為初三／高一／高三各出一份「課堂講義／練習」合訂本封面（docx＋PDF，驗收全 PASS）；過程修好 4 個版面缺陷，其中一個查出 Word 底層的通用地雷。🔴 另揪出一個安全問題：本 repo 係 PUBLIC，而未 commit 的兩個教案有 7 個融合生真名——已攔住不推。**
- **新產出**：`封面_<年級>數學_講義練習.docx/.pdf` × 3 年級（存各年級資料夾根層），建置腳本 `抽離教學_數學/build_cover.py`。
  - 設計經使用者三題一次過裁決：①每年級一份（不分講義／練習）②風格 A「極簡線條」（最貼 house-style）③加學年＋任教老師（細字低調）＋「本冊使用說明」三行。
  - **house-style 偏離已獲授權**：house-style 明文「內頁不要放獨立大標題」，封面必然偏離，本次偏離幅度由使用者選定（見 `build_cover.py` 檔頭註解）。
  - **校名使用者未剔 → 預設不放**（雖然選中的預覽圖有）。腳本留 `SCHOOL = None` 開關，設成字串即加返。
  - 不放頁尾頁碼——封面只有一頁，頁碼是噪音（刻意偏離 house-style 的「固定頁尾」）。
- **修好 4 個版面缺陷**（每輪都出 PDF 目視＋座標實測，不靠肉眼）：
  1. 溢出 2 頁 → 收窄留白。
  2. 使用說明第二行尾「料。」孤行落單 → 該句改寫到 42 字（上限約 46 字，已寫進腳本註解）。
  3. 標題原用全形空格拼「初　三　數　學」——字內與字組間距一樣闊，讀成四個散字 → 改用真正的字距屬性 `track`（`_run` 本來就有這個參數）。
  4. 🔴 填寫欄的下框線離標籤 **21pt**（線浮在標籤下方半格）。**前兩個猜測（`vAlign='bottom'`、固定 `trHeight`）都不是真因，改完零改善**——真因見下面踩坑筆記。
- **填寫欄用真下框線，不用全形底線字元**：`＿＿＿` 在 微軟正黑體→Word→PDF 這條管線上每個字之間有肉眼可見斷口，印出來是虛線不是一條線。
- 🔴 **安全發現：`kongchilok/math-materials` 是 PUBLIC repo**（`gh repo view` 查實 `isPrivate:false`）。而 2026-08-27～29 未 commit 的 `抽離課堂教學教案_初三丙班_全學年.md`（v1.2 註記）與新檔 `課後輔導教學教案_初三數學補救班_全學年.md` 合共有 **7 個融合生真名**。
  - **版控歷史目前零人名**（`git grep` 查過 HEAD），即係一直未漏過，仲截得住。
  - 本次 commit **只推安全檔**（封面 6 檔＋`build_cover.py`＋初三乙班教案）。丙班、補救班、高一戊班（v1.1 註記含一位同事老師真名）三個檔**全部 hold 住等使用者裁決**。
- **未處理（非本輪範圍）**：`高一數學/融合班教學資源/第一學段/01_集合與常用邏輯用語/` 兩個 docx 有來歷不明的未 commit 變動（4983→20143 bytes、99266→130058 bytes），不是今日工作，未動。

---

**2026-08-26：套用兩項新裁決（矩陣／導數確定四校聯考後先開、高三畢業旅行整週外出）重排高三教學計劃，過程中揪出並修好第二學段超編 11-12 節嘅結構性 bug；六班教案（一班一檔、純 Markdown）全部產出並驗證；補寫高三甲班導數缺口嘅正式建議。**
- **兩項新裁決落地**：①矩陣／導數✅四校後（W30）先開，W29 不排新課；②高三畢業旅行✅整週外出，W7 高三兩班節數清零（初三／高一 W7 仍 4 節不變）——`校曆_26-27_上課日曆.md` §三／§六、`排課建議_26-27_抽離時段.md` §零同步改：高三甲 74→**72**節、丙 79→**76**節，合計 499→**494**節。
- **🔴 過程揪到一個非我造成嘅結構性 bug**：高三教學計劃 §4.2／§4.3（第二學段）嘅「索引聚焦操練」同「矩陣／導數」兩行原來共用同一組週次節數，每週雙重計入，兩班合計超編約 11-12 節。發現後未自行猜修，先用 AskUserQuestion 提交具體數字問使用者：現在重建／優先順序（四校前索引、四校後矩陣導數）／12 節缺口點填（裁決：壓縮考試1檢討 22→11節，保住矩陣/導數/索引操練原定節數）。§4.2／§4.3 全部重建，兩班總節數與 §零 對齊、零缺口。
- **⚠️ 新風險：高三甲班導數壓穿下限**（12→10節）——按使用者最終裁決，**已寫入正式建議**（教學計劃 §四.3／§八）：建議甲班抽離時段由週一第一節換去週二或週三空堂，引 §2.4 數字（週一全年 22 天 vs 週二/週三各 27 天），可望追回約 5 節／年，現時已知唯一根治方法。
- **高三 IEP 8/9點覆核**（背景 agent 用 python-docx 逐條查）：揪到 3 處遣詞／內容落差（甲3-6情境應用題；丙9-1/9-2矩陣應用；丙6-2/6-3/6-4空間向量應用），使用者裁決**不改，維持現有寫法**。
- **甲班導數教材口徑確定**：`inclusive-derivative-intro_高三數學` 資料夾查出係空殼，改口徑做「兩班共用 `導數_高三理數學` 資料夾」，已寫入教學計劃 §六。
- **六班教案（教學教案，SEN05式逐課記錄）全部產出**，一班一檔、純 Markdown（`Typedown`＝純Markdown大綱，已同使用者核實定義），存 `抽離教學_數學\_行政表格\`：初三乙(103節)／初三丙(134節)／高一甲(71節)／高一戊(38節，2堂待補)／高三甲(72節)／高三丙(76節)。全部節數獨立用日期算式核對教學計劃，零落差。
  - 初三教案由背景 agent 產出時另外揪到並解決 3 個教學計劃內部排程衝突（W20單元10/11圓冪定理vs正多邊形弧長、W39-41單元17/18丙班週次標記、單元15節數認定），全部依使用者選定嘅 Recommended 方案處理。
  - 初三 agent 一度被 harness 中斷（`status: killed`，非使用者主動），依 [[agent_resume_needs_live_transcript]] 做法冇用 SendMessage resume，改為核實冇殘局後重新 launch 帶齊三項裁決，順利完成。
- **刪走 5 個舊教案 html**（示範填寫×3、高一甲戊班第一/二學段×2）——使用者明確確認「刪」，全部原已入版控可用 git 復原，工作目錄層面已移除。**未刪**：2 個範本檔＋2 個已經一班一檔嘅高三理組/文組 html（超出本輪指定範圍）。
- **收工前順手清走** `_行政表格` 內 5 個背景 agent 遺留嘅 `check*.txt` 除錯暫存（表格行數／pipe轉義驗證輸出，非交付檔）。

---

**2026-08-25：拿到 26/27 官方校曆（v10 老師版）＋六班實際抽離課表，三份教學計劃（初三／高一／高三）＋六份 IEP＋七份教案全部由「25/26 同位推算」重排為「實際校曆＋實際課表逐日實算」；新增校曆解析檔、排課建議檔、高一第二學段教案。**
- **新增兩份基準檔**：`抽離教學_數學\校曆_26-27_上課日曆.md`（官方 v10 校曆逐日機械解析 324 日＋人手核 32 格歧義格，分「中一至中五」「中六」兩軌）；`抽離教學_數學\排課建議_26-27_抽離時段.md`（六班時段整體最佳化，先出量化建議「方案 F」，使用者貼實際課表後再補 §零「已落實時段」對照）。
- **🔴 最大發現：高三（中六）行獨立校曆，v1 假設「同初三高一同一套學期節奏」係錯的**——第一學段考試實際 2026/11/26–12/02（早兩個月）、最後上課日 2027/04/21、統測不參加但一月中學考試期間照常上課。IEP「範圍到 2027/05」要改成 04/21。
- **三份教學計劃重排**（初三 v4／高一 v2／高三 v2）：全部單元分配對住實際課表**剛好用盡、無剩無欠**。關鍵結構性偏差：重陽節 10/19 補假（v1 誤寫不補假）、UT1 統測跨 W10/W11 兩週、初三慈青營由 2026/12 搬去 2027/05–06、第二學段考試推遲到 7/1。
- **六份 IEP 第 8-9 點**：63 處日期按新校曆改（服務期＋112 條短期目標），高三理組重出 PDF（6 頁、1539 中文字無缺字）。過程揪到自己一個 bug——`python-docx` 的 `cell.paragraphs` 每次回傳新物件，用 `is not` 比對會清空啱啱寫入嗰段，六份 IEP 一度全部日期變空白，用 `.bak0` 還原、改用 `_p` 底層比對重做並逐份重讀核實。
- **使用者貼出實際落實嘅六班課表**（人數／原班科任／上課時段）後，全部由「上課日數÷5×週節數」平均估算改為**逐日實算**：五個班節數變多（初三乙 +6、丙 +4、高一甲 +5、戊 +4、高三丙 +2），之前因估算不足要砍嘅內容（高一甲複合函數／反函數／指數對數方程分教、戊班集合元素個數公式／UT1走位／考前複習）**全部恢復**；只有**高三甲攞咗週一第一節（全年最差時段，22 節）令全年少 3 節**，已按 §七 取捨順序壓縮階段一廣泛複習（57→52 節），IEP 點名項目不動。
- **七份教案**（含新增 `抽離課堂教學教案_高一甲戊班_第二學段.html`）全部重蓋日期：逐格驗證日期遞增、星期標籤對正確課表節次、無一堂撞停課日、單元標題週次範圍按實際重算。
- **收尾**：使用者確認刪走 14 個 `.bak0`／`.bak1` 備份檔。
- **仍待處理**：四校聯考確實日期（校曆標「估計 03/18–21」，撞正高三 W29 矩陣／導數開課週）、中六畢業旅行日數（校曆只寫「開始」無結束日）、五份缺簡報（第二學段各科都有，見各教學計劃 §七／§八）。
- **六項待裁決全部有結論**：①2 份空白範本 HTML 已刪 ②76 份舊 HTML 缺頁尾——裁決不做 ③公式紙超頁——見下 ④附件命名——已改名 ⑤高二——維持不在範圍 ⑥`GENRE_RE` 補詞彙卡——裁決不加。順帶刪初三 2 個 `.bak`、退役被取代的 `4.1-4.4` 公式紙。
  - 核實時修正兩個數字：缺頁尾的**交付檔級**係 **76 份**（唔係 81；另外 108 份入面有 23 份 worksheet-tools output、7 份教案、1 份簡報試驗唔算交付檔），初三零缺口；高二零驗算係 **6 個單元 40 對**（唔係 5 個）。
- **公式紙 4 份 → 8 份，全部 ≤2 頁**：01(27條/2頁)、02A(24條/2頁)、02B(8條/1頁)、03A(35條/2頁)、03B(12條/1頁)、04A(16條/2頁)、04B(9條/1頁)、05(2頁)。舊 02／03／04 合併版連 data JSON 退役到 `_archive\退役交付檔\`。
  - **先試壓排版、唔夠先拆**：新增 `"compact": true` JSON 選項（opt-in，儲存格上下內邊距 50→30 twips、章節標題留白減半，**只動留白唔動字級**——公式紙要帶入考場，縮字直接影響可讀性）。壓完 02 由 811→635 字、03 由 782→579、04 由 297→238，**三份都仍然 3 頁**，先至走拆張。
  - **三組拆張全部機械核實零內容改動**：舊檔與拆出兩檔的 (節標題, name, formula, note) 四元組逐條比對，02（32→24＋8）、03（46→34＋12）、04（25→16＋9）全部 `== True`。
  - QA：**88 項檢查、87 PASS、1 略過、FAIL 0**（起步時 5 FAIL）；`validate_data.py` 8 檔全 PASS、問題數 0；八份 PDF 中文抽字 241～926 字無缺字。
- **改名 05 立即抬出三個藏咗 4 個月嘅缺陷**：`附件_立體幾何初步公式_高一數學` → `公式紙_高一數學_05立體幾何初步`。舊檔名「附件_」唔喺 `qa_check.py` 的 `GENRE_RE` 內，**公式紙專屬檢查（QB-13a／13b、QB-4）由頭到尾冇套過落佢**——①多咗一頁全空白 ②冇頁尾頁碼 ③**冇「本頁允許攜帶入場」識別字樣**（公式紙最核心嗰行，冇咗監考員冇依據放行）。三個都修好，六張示意圖統一縮到 85% 換位，最終 2 頁、圖全在、公式一條冇少。
- **自己揪返兩個錯**：①**去重去多咗**——本來以為 02 的「負指數冪換算」已被 03A 涵蓋，核對後發現 03A 只有「負分數指數冪」`a^(−m/n)`，**成份公式紙冇整數負指數** `a⁻¹=1/a`、`a⁻ⁿ=1/aⁿ`，照原計劃刪落去會由兩張紙同時消失，已補返落 03A「4.1 指數運算」。②**`{Delta}` 地雷**——改 04 時一度寫 `{Delta}`，唔係支援 token，會原樣印出五個斜體字母（同記憶 `docx_math_markup_landmines` 一模一樣），兩層檢查都攔唔到，靠出 PDF 前逐字核對揪返。
- **NotebookLM「高一數學融合班教學資源 ④」已同步**：原本 3 個來源**全部**係本輪退役／改名的舊檔，而現行公式紙一份都未上傳。先加 16 個（8 份 × docx＋pdf）→ 核數 3→19 ✅ → 刪 3 個過期來源 → 最終 `source_count = 16 = 3＋16−3` ✅。
- **試過但放棄一招**：compact 另設欄寬（名稱 1900／公式 6100／說明 3338）想放闊公式欄少斷行——**反效果**，02 溢出由 423pt 變 458pt（名稱欄收窄令「五個常見冪函數的性質」那類長詞多斷一行，抵銷有餘），04 零改善。已還原，腳本內留註記免得日後再試一次。
- **已開背景任務 chip**：`task_378b3b5f` 建議替 `{}` 標記語法補「未知 token」白名單檢查（`validate_data.py` 同 builder lint 都攔唔到 `{Delta}`／`{mathbb(R)}` 呢類）。

---

**2026-08-21：高一鐵律5驗算缺口清零（6份新驗算檔／20對教材／約187個小題）＋揪出並修好5類真缺陷（1個數學錯誤、2份PDF渲染損壞、2處LaTeX標記外露、3份講義範例遺失、教案4處殘留標記）；順帶修好QA假警報與5個腳本死路徑。**
- **起點是「單元06圖像法解不等式」查證，結果是假缺口**：教材 2026-08-14 已整套補齊（講義§三＋練習9-13題＋工具卡＋NotebookLM同步＋commit `6608e01`），工作筆記那條是 08-06 登記後沒更新的殘留。真正未做的是**教案上 4 處殘留標記**——`抽離課堂教學教案_示範填寫_初三乙丙班.html` 的「⚠️ 教材缺口（尚餘一項）」、11/12 該節「⚠️ 待補」欄、11/16 的 C7/C8 舊講法（08-17 已改用韋達定理＋完整配方）、頁尾「全套教材唯一沒有現成材料的一堂」＋退讓方案，全部更新為已閉環。
- **高一鐵律5缺口清零**：原有 4 個資料夾**完全沒有**驗算紀錄，另 2 個**只部分覆蓋**（第一輪只掃「零驗算檔」的資料夾漏了後者，第二輪靠 QA 的 QB-11 揪回來）。逐題親自重算，新增 6 份驗算檔：立體幾何(1對/11題)、三角函數(3對/34題)、集合與常用邏輯(4對/36題)、平面向量與正餘弦(5對/40題)、04多項式因式(3對/16題)、03指數對數(4對/50題)。高一 QB-11 由 9 個 FAIL **清零**。
- **真缺陷① 數學錯誤（1個）**：`練習_角與三角函數的概念` A-1 答案寫「與 60° 終邊相同的是 −300° **和 480°**」——480°−60°=420° 不是 360° 的整數倍（480° 其實與 120° 同終邊），正解只有 −300°。**只改答案不改題**（480° 是好誘答，逼學生真的執行提示所說的檢查；若改成 420°，上一行已算過，題目就白給了）。源頭 `build_g1.py`＋`gen_html.py` 各一處，重出 docx/html/pdf，逐頁比對確認只有答案頁那一行變。
- **真缺陷② PDF 渲染損壞（2份）**：`練習_充分條件與必要條件.pdf`（55% 中文缺失，「教師用參考答案」印成「& ” x 6 f」）、`講義_空間推理_抽離小班共用版.pdf`（math-test，90% 缺失）。字型子集嵌入失敗，**HTML 源檔完好**，以無頭 Chromium 重印＋轉圖目視確認。自寫偵測法（HTML 中文片段 vs PDF 可抽字比率）掃全工作區 350+ 對 HTML/PDF，確認只有這兩份中招。
- **真缺陷③ LaTeX 標記外露（2檔）**：`講義_4.4對數函數`、`練習_4.3反函數` 印出 **𝑚𝑎𝑡ℎ𝑏𝑏(𝑅)**。docx 走 `{}` 標記語法而 `mathbb` 不是支援的 token，原樣吐出字母（同 `{Delta}` 變五個斜體字母同類）。HTML 走 LaTeX 由 MathJax 渲染故正常。改 `{R}`，重出 docx＋用 `office-pdf-convert` 重出 PDF。全工作區掃 285 個 docx 只有這 2 個。
- **真缺陷④ 講義範例整段消失（3檔）**：`講義_指對數運算`／`_指數方程`／`_對數方程` 的 docx「三、範例」下只剩 **`wwwww`** 五個字母。三個腳本把**已砌好的 para() 段落清單**傳給 `shaded_box()`，每段靜默塌成第 2 個字元 `w`。**共用底層 `omml_docx.py` 早在 2026-08-08 就記錄這個坑並提供 `shaded_multi_box()`（註解寫明就是在本資料夾發現的），但 topic2/3/4 一直沒跟著改**。已改用 `shaded_multi_box()` 重出，三份講義各恢復一段完整範例、三份練習零差異。
- **工具修正**：①`math-deliverable-qa` 的 QB-14 白名單補入 `code`／`clipPath`／`mask`／`mjx-container` 等合法標籤——12 個 FAIL 全是假警報（7 份教案用 `<code>` 標檔名、幾何圖用裁剪遮罩、MathJax 渲染輸出），現在 0 個。②修好 5 個建置腳本的死路徑：`common.py`／`gen_html.py`（指向舊機「任務\融合版_三角函數」）、`build_inverse`／`build_logarithm`（指向舊機「高一講義\…」）、topic2-4（`OUT` 指向 `_scripts/` 自己那層）。
- **核實沒問題、不用再查**：鐵律2 雙格式——10 個「缺 PDF」全部是原材料／IEP／含學生真名檔，教材交付檔零缺口；工具卡 OMML——25 份全部有 OMML、零空殼，08-12 那條待辦已閉環；QB-2 的 9 個 `m:oMath×0`——5 個是 08-17 已核准的假警報，其餘是原材料。

---

**2026-08-19：清三項久懸裁決（同質分組／LaTeX bug／舊簡報回溯），規則寫入 skill；登記新學期 IEP・教學計劃・教案更新待辦。本日零教材產出，屬決策與規則沉澱輪。**
- **同質分組議題結案（原「唯一一項使用者主動押後嘅設計議題」）**：使用者說明實際教學負荷——2026/2027 學年**三個年級、六個班、每週 16 節、共 17 名融合生**，並指極端落差個案「狀況少，而且成本未能負擔」。裁決：**唔開同質分組產線**，產出模型固定為「一份講義照顧全班」，差異化仍由三層難度＋D1–D15 鷹架承載。
  - 規則已寫入 `inclusive-math-worksheet-generator`：`references/teaching-designs.md` 新增 **§2.1「落差過大時：加補丁，不開分組」**（預設立場＋產能理由＋三級補丁階梯＋三條「不要做的事」）；`SKILL.md` 步驟 3.5 加 ⚠️ 交叉引用，**禁止再把「按能力分組、各出一套教材」列為提問選項**。
  - **補丁階梯**（成本由低至高）：① 取現成低一級／低一屆教材當替代練習 → ② 加一頁先備技能補充（一至兩個 worked example）→ ③ 落差 ≥4 個 L 級才升 `macau-iep-math-goals` 第 8 點抽離軌。關鍵區分：階梯 1、2 係「一份主教材＋一份補丁」嘅個別調適，**唔等於開分組**（多份平行主教材）。
  - 特別明文寫成「**成本約束下的既定裁決，不是每次都要重新權衡的設計選項**」，避免將來又攞出嚟問使用者一次。已存記憶 `teaching_load_no_ability_grouping.md`。
- **01 單元練習第 11 題 LaTeX 缺字 bug：核實早已修好，唔係未完成項**。用 `git log -S` 精確定位——bug 由 `eed5901`（2026-08-04）引入、`c87a931`（2026-08-05）修正，同時跟返 house-style 將 `\pm` 拆做 `x₁`／`x₂` 兩支。**做咗目視驗證而唔淨止睇原始碼**：PDF 第 8 頁渲染正確（`(x+5/2)²=17/4，x₁=(−5+√17)/2 或 x₂=(−5−√17)/2`，`\left…\right` 括號正常撐大），全檔零殘留裸 `ight`；重新驗算題目 `x²+5x+2=0` 配方得 17/4 ✓。背景任務 chip `task_b9592fe6` 已隨 app 重啟失效（同 `task_58c935b5` 同款），dismiss 唔到亦唔會再觸發。
- **舊 8 份簡報斜體／底線回溯統一：使用者裁決暫時不做**，維持現狀（原為全工作區級改動）。
- **關鍵詞彙卡存檔位置查明＝零落地**：全 repo `find`／`Glob` 搜唔到任何詞彙卡檔案，`模板試驗/_verify_詞彙卡` 係**空資料夾**；2026-08-18 嗰個單元 01 試範樣本**只存在 Artifact**（`一元二次方程詞彙卡`，私有連結未分享，2026-08-17 更新）。即係呢個「常設產出」至今**未做過任何正式交付檔**，同 08-17「批量推廣暫不急做」嘅裁決一致。
- **新學期待辦已登記（等校曆）**：核實三個年級 IEP／教學計劃／教案草稿齊全、六個班對得返教學負荷（初三乙丙／高一甲戊／高三甲文丙理）；高二有 IEP 草稿但無教學計劃、無教案，判定備用、不在範圍。詳見下面「下一步」。

---

**2026-08-18：18個初三單元驗算紀錄鐵律5缺口全數清零（新建15份、查缺補齊3份）；順手修好單元06 C7/C8答案改用已教工具、單元10補公切線長度內容缺口；NotebookLM同步；關鍵詞彙卡試範樣本。**
- **驗算紀錄缺口清零**：連同前已完整嘅01單元，全部18個初三單元、逾190項計算現已100%逐題重算並記錄。派17個subagent分頭讀PDF逐題重算——14個單元由零新建（02/03/07-18），04/05/06三個單元查缺補齊（原檔只覆蓋部分內容，例如05原本淨係補咗§三/§六、06原本淨係補咗§三圖像法解不等式）。全部同文件答案一致，**零真數學錯誤**。
- **順手揪到並修好2個真缺口**：①單元06練習C7/C8答案直接用咗「交點式」「最值公式c−b²/4a」呢兩個講義未教過嘅工具（教案第766行早有記錄呢個已知問題，退讓方案寫明「未教過則改用配方硬做」）——改用單元02已教嘅韋達定理（C7：由兩根1、−3反推b=2,c=−3）同完整配方法（C8：a(x−2/a)²−4/a+c逐步展開推a=1,c=−1）重新推導，數值答案完全不變。②單元10題為「兩圓的公切線」但內容零覆蓋公切線長度計算，講義標題承諾嘅內容冇兌現——新增「四、兩圓的公切線長度」一節（畢氏定理推導＋雙面板示意圖＋worked example用5-12-13乾淨三角形）連練習2題，沿用**D7提示卡**設計（原講義冇標註設計代號，本次對照教學結構補上）。兩者都已重出docx+pdf、`math-deliverable-qa`驗收、更新驗算檔。
- **NotebookLM已同步**：「初三數學融合班講義練習(上)」筆記本先加後刪替換6份舊來源（單元06練習、單元10講義+練習各docx/pdf），source_count 60→60核實正確。
- **關鍵詞彙卡試範**：用單元01出咗一張常設產出樣本（Artifact預覽）畀使用者睇效果，使用者裁決全工作區18單元批量推廣暫不急做。過程中修好一個Artifact工具嘅CSP誤判——MathJax CDN喺Artifact讀唔到（Artifact要求完全自足、唔准連外部CDN，同`mcp__visualize`widget嘅CDN allowlist規則唔同，兩者容易混淆），改用純CSS/HTML手砌數學符號（疊行分數、頂線根號、原生上下標）解決。
- **過程小插曲**：C7/C8同單元10嘅subagent中途撞正系統session usage limit中斷，用SendMessage resume失敗（transcript唔存在，session limit中斷嘅agent冇辦法resume）——改為重新launch，加咗「先檢查有冇殘局至決定接手定由頭做」嘅指示，兩個都順利接手完成，數值/QA核實冇受影響。

---

**2026-08-17：高一公式紙補PDF＋QB-21/22/23三條新QA檢查真實驗證（純檢查揪到3個真發現）＋修好公式紙04真bug並同步NotebookLM。**
- **高一公式紙缺口補齊**：`附件_立體幾何初步公式_高一數學.docx` 一直冇對應PDF（違反鐵律2），
  用 `office-pdf-convert` 一次成功轉出（3頁），鐵律2缺口清零。
- **QB-21/22/23 真實PDF驗證＋合成測試雙重確認**：對初三18單元全量掃描（`--legacy-ok`），
  三條規則本身邏輯全部正確——QB-21 全部正確WARN（有條號/基力以外→PASS，兩者皆無→FAIL，
  三種破折號/單雙位數條號全過）；QB-22／QB-23 在這批單元完全零觸發（真實內容沒有可測案例），
  改用合成字串測 function 補上正/負案例覆蓋，全部符合預期。過程中一度誤判QB-21有假陽性，
  查證後是自己測試字串意外包含目標子字串，非腳本問題。
- **同一次全量掃描順手揪到3個超出任務範圍的真發現（A/B/C，使用者裁決）**：
  A：關鍵詞彙卡「常設產出」全工作區零落地（18單元一份都未出，`GENRE_RE`亦未收錄該前綴）；
  B：14個單元（02/03/07-18其中多個）練習資料夾完全冇 `驗算*.md`，另4個有檔案但覆蓋不全；
  C：QB-2「m:oMath×0」6個FAIL逐一查證。**A、B使用者裁決不急做，本輪未處理。**
- **C 已修復**：6個FAIL中5個係假警報（07旋轉與中心對稱、18投影與三視圖的講義/練習/公式紙，
  內容本身是坐標描述句/幾何定義，不需要OMML，跟已核准的13單元「純坐標保持純文字」慣例一致）；
  **只有公式紙_04（分式方程無理方程與二元二次方程組）是真bug**——三個分區的表格「公式／定理」
  欄整欄空白，「名稱」欄被「解法步驟」字眼頂替，完全冇任何公式。用 `jh-math-formula-sheet` 的
  正式 `build_formula_sheet.py`（非手改XML）重新產出，三條公式直接沿用講義《範例一二三》
  已驗算過的原題（3/x=x−2、√(x+2)=x、y=x+1與x²+y²=25），QA 11/11全過。舊版備份為`.docx.bak`
  /`.pdf.bak`（使用者選擇保留備份而非直接覆寫）。
- **NotebookLM同步**：發現公式紙其實屬於獨立筆記本「初三數學融合班講義練習(工式紙)」
  （36源），跟「(上)」筆記本是兩本唔同的（一開始假設錯咗，查證後改正）。先加後刪替換完成，
  過程中PDF第一次上傳報連線錯誤，重試後核數揪到一個重複source（第一次其實伺服器端已成功，
  只係回應冇送到）——已刪走多餘嗰個，最終核數36→36正確。
- **新增2條記憶**：QB-2「m:oMath×0」兩種成因（內容本身唔需要公式 vs 公式欄真係冧咗，
  唔可以一見FAIL就照單全收去改）；NotebookLM `source_add` 報錯可能其實已成功（重試後要核
  重複，唔止對總數）。

---

**2026-08-17（同日第二輪）：`worksheet-tools`（四校聯考數學工作紙）教師版全面稽核書寫規範，10 年份共 49 處違規逐輪修正並重出；順帶統計三個年級雙面印刷用紙量。**
- **起因**：使用者要求查 `worksheet-tools/output` 2017-2026 有冇嚴重超出書寫規範。查證後發現
  呢個工具建於 2026年6月尾，**早過 2026-08-05 先定案嘅「拆支寫或/±拆支」規矩**，教師版
  `solutions/sol*.js`（10 年份、3380 行）從未跟過——即係一個獨立於 `inclusive-math-worksheet-
  generator` 管道之外、但同一套書寫規範精神都應該套用嘅工具，一直冇人做過事後對齊。
- **分 3 輪逐一核對並修正，共 49 處，橫跨全部 10 個年份**（每輪改完即 `node -c` 語法檢查＋
  `build.ps1` 全年重出 HTML/PDF＋瀏覽器抽查 MathJax 零渲染錯誤）：
  1. **拆支寫或/±拆支**（18處，9/10年份）：`a=3或-2` 單行散文改 `a₁=3　或　a₂=-2`；
     2024/2025 三處最終答案留低未解嘅 `±`（`a=10±2√2` 類）展開成兩支。
  2. **判斷混入算式行**（4處，2017×2/2018×1/2025×1）：驗證結論（合/捨/恆成立）搬去
     `reg()` 標題，算式行淨返純計算。
  3. **分數指數冪/對數底扁平斜線**（11處）改 `\tfrac` 疊字（`7.2^{1/3}`→`7.2^{\tfrac13}`）。
  4. **複合分數內層扁平斜線**（8處）改完全疊字巢狀。
  5. **不等號換等號要獨立一行**（8處，2019/2023/2024/2025/2026）：因式分解完直接跳解集，
     缺咗「令乘積=0求分界點」嗰行，逐一補返。
  - **「一行一個等號」規矩本身有76+處違規**（會令教師版詳解加長三四成）——**使用者裁決
    唔跟**，維持現狀不改；6處不等式解集聯集（`x<2或x>3`類）本身合規，唔屬呢批規矩管轄。
- **順手統計雙面印刷用紙量**（使用者另外問）：
  - `worksheet-tools` 學生版工作紙 2017-2026：132頁，分年份獨立印每年7張，**10年一套70張**。
  - `抽離教學_數學` 講義+練習（除高二）：初三36份150頁81張、高一58份197頁108張
    （第一學段74/第二學段34）、高三36份128頁74張，**合計130份475頁263張**。
- **Git**：`worksheet-tools/solutions/`＋`output/` 47檔已 commit（`588295a`）並 push。

---

**2026-08-15：repo 級 git 衛生補課（772 檔一次性入版控）＋初三書寫規範批量套用結案＋高一 02 單元內容搬回正確位置。**
- **repo 級 git 衛生補課**：開工發現 109 個未追蹤項目（大部分係歷史累積、從未 commit）。逐一核查
  後：`.claude/worktrees/`（現正使用中嘅獨立 git worktree，自己有 `.git`，唔可以當普通檔案入版控）
  同 `math-test/檔案_000~0003`（4 個 zip、共約 850MB 原始 export dump，內含 GUID 命名大型 PDF，
  單檔已超 GitHub 100MB 上限）落 `.gitignore` 唔入版控；其餘 772 個檔案（`抽離教學_數學/`、
  `math-test/` 題庫、`worksheet-tools/`、`模板試驗/`、3 個新工具資料夾、`.obsidian/`、`.mcp.json`、
  `.claude/launch.json`+`settings.json`）一次過 commit（`c03e5e7`）並 push（`f2b4af9..c03e5e7`）。
  過程撞到一次 stale `.git/index.lock`（14分鐘前殘留、`tasklist` 確認冇 git process 跑緊），
  安全刪除後重試成功——[[git index.lock 卡住時先確認冇process先刪]] 呼應舊踩坑筆記。
- **書寫規範批量套用（等號對齊/拆支寫或/∴結尾）正式結案**：逐一核實剩低嘅 9 個初三單元
  （01/03/04/07/13/14/15/17/18，讀 PDF 逐頁核對）——**01、03、04 原來已經完全合規**（前幾輪
  工作筆記漏記，03 教師頁仲寫低咗「三條書寫規矩不印在教材上」呼應 house-style §6）；
  **07/13/14/15/17/18 核實後確認本身唔適用**（坐標描述句／連比例展開／單步代入計算，
  同 16 單元「判定唔使改」屬同一類，強行套會教錯）。連同之前已做嘅 8 個單元
  （02/05/06/08/09/10/11/12），**18 個初三單元全部完成／判定不適用，冇一個剩低**。
  練習答案區（約39處）同 01 單元「三條規矩」條文——使用者本輪再次確認裁決不套用，
  維持之前決定。冇檔案改動。
- **高一 02_函數的概念與性質 結構修正**：使用者要求檢查高一資料夾，發現單元 02（5 個子題：
  函數的概念與定義域／函數的表示方法與分段函數／單調性與奇偶性／複合函數／冪函數的性質）
  內容其實齊全（講義+練習 docx/html/pdf ＋ 驗算.md），但全部埋咗喺
  `_scripts/融合班講義/` 底下，冇好似 01/03/04 咁直接放喺單元資料夾根目錄——單元資料夾表面
  睇好似冇做過。已用 `git mv` 搬返 35 個交付檔去單元根目錄，build script 留喺 `_scripts/`。
- **順手發現、使用者本輪選擇未處理**：`公式紙/附件_立體幾何初步公式_高一數學.docx` 冇對應
  PDF（違反鐵律2 雙格式交付）。

---

**2026-08-14（同日第四輪）：`~/.claude/skills/` 搬去 GDrive 建立版控備份；補完選項(c)法規／校規，2(c) 全部閉環。**
- **skills 資料夾風險解除**：原本喺 C 碟未進版控、唔喺 GDrive，已 robocopy 驗證（377/377檔案、0失敗）搬到 `G:\我的雲端硬碟\2ndBrain\_claude-skills`，C 碟原位改做 **junction**（非 symlink——呢部機開唔到 symlink 需要嘅權限，junction 唔使 admin、效果一樣，已測試功能正常）。順手起咗個小 git repo（本機、冇遠端——GDrive 本身就係異地備份），已 commit 3次（`fc8bb16`初始化／`6add22f`／`42dc729`鎖定法規條號）；`rdq` 子資料夾原有獨立 `.git`（自己嘅 GitHub 遠端 `mathruffian-dot/rdq-skill`）已拆走折埋做普通檔案，唔重複維護兩層版控。
- **選項(c)三項全部閉環**（改喺 `macau-iep-math-goals/references/iep-goal-rules.md` §0.5，commit `6add22f`＋`42dc729`）：
  1. 29/2020 融合生評核依據＝**第二十三條第一款**（官方逐字核對，唔係 WebSearch 摘要）
  2. 28/2020 全19條親自逐字讀過（`rega-28-2020.pdf`，使用者提供），揪到**第七條「特別評核」正是 IEP 機制本身嘅法定定位**（「制訂、檢討和修訂學生的個別化教育計劃」）——呢個係額外收穫，之前個 skill 完全冇呢條引用
  3. 融合生升留級跟一般規則，法源係**第十一條**（比率上限：小一至四不留級／小五六≤4%／初中≤8%）同**第十二條**（特殊情況申請），**唔係**先前以為嘅第十五條（該條實為程序性條文，規定學校自訂校本評核規章，唔含實質留級比率）；校規引用第十五條本身冇錯（嗰係其立法授權依據）
  4. 附帶更正一個舊筆記猜測：《學校運作指南》第三章第五節「教和學」核對後證實與升留級無關（純教學人員操守指引），之後唔再引用呢份文件講升留級
- **`task_58c935b5`**：使用者確認已手動修完（見下面第三輪紀錄），嘗試 dismiss 個 UI chip 但 task_id 已隨 app 重啟失效——筆記早已標✅，UI chip 可手動關，唔會再觸發動作。

---

**2026-08-14（同日第三輪）：讀完 358 頁兩份《數學課程指引》＋兩份《基本學力要求》，補實研讀筆記，並把 §9 建議清單 12 項全部落地到 5 個 skill。**

- **選項 (b) 完成——課程指引挖到三件事**：
  1. **年級序列的法源係《澳門初中數學大綱》(1999) 表6**，唔係基力。課程指引自己明寫「至今初中學段仍沿用 1999 年頒佈的《初中數學大綱》」。43 個單元逐年級全表已抄入研讀筆記 §3.4。
  2. **高中冇文理分流、冇年級表**。全文所有「必修／選修」都係講外國。高中基力自述係「所有學生的共同數學需求」＝單一共同地板，理組加深屬校本。
  3. **積分／空間向量／矩陣唔喺高中基力，導數喺（D-1，5條）**——課程指引「內容框架」同基力兩份互相印證。
- **推翻咗自己上輪一個結論**：研讀筆記 §3.3 原本有一欄「≈年級」（L16=初一、L17=初二、L18=初三），係照人教版序列推嘅。對回澳門大綱表6 之後發現**逐年推法系統性出錯**（因式分解／分式／多項式喺初一、畢氏定理同頻數分佈喺初二，同原欄位相反）。**整欄刪除**，改用大綱表6。連帶把**落差起算基準統一改為「所讀學段正規帶下限」**（初中＝L16 而唔係 L18），所有 skill／規格草案／試跑紀錄嘅數字同標籤全部對齊（試跑個案由 6/5/6 級改為 4/3/4 級，結論不變）。
- **意外收穫：澳門有自己嘅官方認知動詞四級**（瞭解→理解→掌握（能）→運用（會用，證明），另有過程動詞 體驗（體會）／感受）。**基力每一條都係用呢套字寫**，而且**同一條文內常混用唔同層次**（例 `A—2—8` 前半「掌握」後半「瞭解」）——即係基力自己已經標好邊部分要到咩深度，呢個就係課程調適「邊度可降、邊度唔可以降」嘅官方依據。既有筆記庫只有 Bloom 六級。
- **12 項建議全部落地**（研讀筆記 §9 執行次序已全標 ✅）：
  - `macau-iep-math-goals`：新增 **§K 認知層次**（四級定義／長短期動詞分工／同條文混級＝調適刻度／基力內外決定調適正當性）；A.1 加交叉引用；G.1 落差起算基準；`lapl-math.md` §2.1 大改。
  - `inclusive-math-worksheet-generator`：步驟 3 **新增必填「本單元對應的正規教學目標」**（照抄基力原文＋條號）；新增「**附加產出**」節（官方附錄四.2 教學計劃／四.1 例證紀錄表，**預設不做、觸發詞出現先問**）；步驟 2.5 加 3.5 小步（查落差決定要唔要走 D15）。
  - `teaching-designs.md`：**新增 D15 同情境分任務**（官方第五章範式，含高中貨幣舉隅全表）——**唯一允許唔標星星／唔標初中高階嘅設計**，理由係社交隱蔽性；新增 §1.1（S碼↔三範疇↔L帶）；§2 加「典型起點 L 帶」「組內落差」兩列；D8 加官方上限 **≤10 個詞**。
  - `math-deliverable-qa`：`qa_check.py` **新增 QB-21（講義正規教學目標＋基力條號）／QB-22（關鍵詞彙≤10）／QB-23（目標結果導向）**，已編譯＋逐項煙霧測試通過（特別驗證 QB-23 **唔會誤報「掌握／瞭解」**）；`GENRE_RE` 加 `教學計劃`；QB-V5 加 D15 四條專項。
  - `math-visual-scaffold-generator`：**單元關鍵詞彙卡升為常設產出**（官方要求課室張貼、≤10 詞）。
  - `notebookLM\CLAUDE.md` 路由表補兩行。
- **順手修好一個靜默 bug**：4 個 skill 檔共 **8 處引用嘅筆記路徑係死路徑**（寫 `G:\我的雲端硬碟\2ndBrain\每日筆記\`，實際係 `G:\我的雲端硬碟\AI learing\2ndBrain\每日筆記\`，中間漏咗 `AI learing` 一層）。呢個會令 worksheet-generator 步驟 0「一定要讀」嘅三份主依據**靜默讀唔到**。已全部修正，CLAUDE.md 路由表加咗警告。（即係 `task_58c935b5` 想做嘅嘢，已手動做完，可以 dismiss。）
- **選項 (a) 早已完成**：兩軌設計上輪已施工並試跑，本輪只做數字對齊。**選項 (c)（等使用者補法規／校規）仍係唯一未完成項**，性質係等資料唔係等做嘢。

---

**2026-08-14（同日第二輪）：IEP「兩軌設計」——解決融合生「IEP標低階可過、但原班測考通唔過會留級」嘅核心矛盾，重寫 `macau-iep-math-goals` skill。**
- **問題根源**：舊 skill 將「第9點長期目標」同「學生真實能力」寫成同一件事，但融合生課程目標法定要求同原班一致（唔可以剪裁），而佢哋嘅能力普遍落後原班——兩把唔同嘅尺被逼疊成一把，永遠顧此失彼。
- **解法＝兩軌分工**：第9點＝**原班軌**（每條長期目標附基力條目編號，錨原班進度，落差幾大都唔改動——課程調適一律勾「與普通生課程無異」）；第8點＝**抽離軌**（四欄格式：目標／支援邊條長期目標／橋接嘅具體先備技能／位置，用嚟補落差，唔係抄薄原班目標）。落差 ≥4 級觸發**安置預警**輸出（唔係自把自為剪裁課程）。
- **新增兩份查表工具**（`~/.claude/skills/macau-iep-math-goals/references/`）：`bac-index.md`（基本學力要求全文謄錄，初中136條+高中143條）、`lapl-math.md`（學習能力進程階梯 L1–L18 三範疇描述＋5條使用禁區）——**首次有根有據咁確認高中無 L 級對應係設計使然、唔係文件缺漏**（高中用基力條目自己嗰把尺，落差先用 L 級）。
- `iep-goal-rules.md` 全面改寫（新增 §0 兩軌設計、§B.1–B.4、§D 課程調適、§E 第8點四欄規格、§J 階梯用法禁區）、`SKILL.md` 逐步驟同步改。
- **真實單元試跑**（初三上學期一元二次方程／判別式／二次函數）揪到並修好 2 個設計 bug：①L跨度規則原本掛第9點——但第9點永遠錨原班、跨度恆定，規則形同虛設，已移到第8點；②跨度超標原本當「打回重來」——但試跑個案 4 級跨度每一環都必需（求根公式必然出負數），刪咗等於明知學生做唔到都送佢去考，已改為「訊號」：寫入預警具體理由、唔刪必需環節。
- **順帶查到嘅談判籌碼**：韋達定理、相交弦定理、切割線定理（初三教材）、積分／空間向量／矩陣（高三教材）全部**唔喺基力範圍內**——呢啲係校本加深內容，融合生喺呢啲內容嘅測考調適正當性明顯高於基力以內。
- **呢批檔案唔喺呢個 repo 入面**：skill 檔案喺 `~/.claude/skills/macau-iep-math-goals/`（本機 C 碟，**未進版控、亦唔喺 GDrive**——換機或者呢部機出事會冚檔），研讀筆記同設計規格喺 `AI learing\2ndBrain\每日筆記\3.0\`（GDrive 自動同步）。
- **未解依賴**：教青局第29/2020號批示嘅融合生評核條號未鎖定（`bo.io.gov.mo` 本機DNS唔通）；358頁《中學數學課程指引和基本學力要求》逐年細分同文理分流細節未讀；已 spawn `task_58c935b5` 修另外兩個 skill（`inclusive-soil-teaching-deck`／`inclusive-math-worksheet-generator`）引用嘅死路徑。

---

**2026-08-14（同日第一輪）：覆核＋收尾昨日書寫規範改版（刪8個.bak、修05表格跨頁）；全工作區排查並修好5個工具卡OMML缺失；補齊06單元「圖像法解不等式」教材缺口（丙班獨有，教案標注已久）；NotebookLM三個筆記本同步。**
- **收尾覆核**：8 個 `.docx.bak` 原稿備份已刪（02 舊版 PDF 按使用者指示保留）；05 §六 範例2 表格跨頁——`shaded_box()` 新增 `page_break_before` 參數（共用layout層，冇動 `_shared-math-docx` 底層），總頁數 8→7，數學內容零改動。
- **練習答案區清單**：背景 agent 掃描 18 份練習答案區，實際找到 **39 個位置、約52個違規片段**（比原估「約25處」多，落差來自幾何/概率/三角單元嘅一步式算式鏈）。**使用者裁決：暫不處理**（工作量大、教師簡答文體本身唔追求同範例段一樣嚴謹），清單已存在對話記錄，未來如要做可重新掃描。
- **工具卡型交付檔 OMML 全工作區排查＋修復**：查咗 24 個工具卡 docx，用 Sonnet 背景 agent 修復：
  - **3 個中招**（同高三導數個案同款bug——部分公式已用OMML、部分打字冇轉）：`math-test/materials` 嘅「二次方程與不等式」「直線與圓」「圓錐曲線」速查卡，判別式/兩點距離/點到直線距離/橢圓關係等公式已轉真OMML。
  - **2 個新類型缺口**（連打字公式都冇，純步驟詞）：高三理科「不定積分/定積分手順卡」，已補實際公式（冪函數積分公式、牛頓萊布尼茲公式）。
  - 5 個檔案 30/30 QA檢查過，agent自行揪到2個間距回歸並修好。
- **06單元「圖像法解不等式」補齊**（Opus 背景 agent）：查證後發現唔係編號撞號誤會，係**同一課入面漏咗一節**——丙班5節比乙班多一節，教案自己標「⚠️待補」，排定 2026/11/12。已補：
  - 講義新增 §三（概念對照表→D2手順卡→兩個worked example用`worked_example_table`+`dual_track_table`雙軌對照→開口×不等式決策表），§一/§二原文逐字未改（程式核對）
  - 練習新增第9-13題（3層鷹架：由圖讀解集全給提示→半填空→由式畫圖零提示），含教師答案
  - 新工具卡《圖像法解不等式手順卡》（四步手順+決策卡）
  - 逐題親自驗算全過，14張圖PNG目視核對全過
  - **PDF一度轉唔到**（Word COM PDF匯出間歇性RPC失敗，一度誤判為「PDF引擎壞咗」需要改預設印表機），**後來重試即成功**——見下面踩坑筆記，不要過早下「環境級故障」結論。
- **NotebookLM 三個筆記本同步**：初三數學融合班講義練習(上) 58→60（06新增3類檔案）、題庫暨教學材料生成專案(高三融合班) 64→64（3個工具卡替換）、高三數學[理組]積分融合班教學資源 12→12（2個工具卡替換），全部 `source_count` 核實。
- **Git commit `6608e01`已push**：26個檔案（05/06單元+3個math-test工具卡+2個積分工具卡）。

---

**2026-08-12（同日第三輪）：初三 8 份講義嘅範例段套新書寫規範（等號對齊／∴答句），13 個範例段改用七小欄表，順帶揪出並修好 2 個真 bug。**
- **範圍係使用者當場裁決收窄嘅**：原本問題係「其餘 16 個單元要唔要批量套新書寫規範」。先做全庫掃描，發現初三符合模式嘅有 53 處，但**約 15 處係假警報**——01 工具卡 `x=4+3=7` 同 01 練習找錯題 `(x+3)²=5+9=14` 係**故意寫錯嘅反例／題幹**，另有重根 `x₁=x₂=2`、連比例、`sin A=對邊/斜邊=a/c` 定義展開，**呢啲改咗反而教錯**。使用者最後揀「**只做講義範例段**」（約 13 處），並明講「01 唔使清」（指嗰 15 處三條規矩條文）。
- **改咗 8 個單元**：02（判別式①②③＋兩根和積）、05（§五配方）、06（§一交點＋§二最值）、08（§二垂徑＋§四圓內接）、09（§三切線）、10（§一相交弦＋§二切割線）、11（§三弧長＋面積）、12（§一擲骰＋§二拋硬幣）。13 段最終答案**與舊版完全一致**，逐條重算記錄喺 `驗算_書寫規範改版_2026-08-12.md`。
- **順帶揪到 2 個真 bug**：①**02 §一、06 §一 嘅 Δ 同等號原本係打字文字、唔係 OMML**（違反鐵律1）——同第一輪喺高三工具卡揪到嗰個係同一類地雷，證實「其他舊教材可能有同類問題」呢個懷疑成立；已轉真公式（OMML 02:14→80、06:6→56）。②02 韋達定理通則行寫 `frac(-b,a)`（負號縮埋落分子），同新範例嘅 `-frac(b,a)` 打架，已統一（house-style §7/§8）。
- **一個教學判斷**：幾何長度開平方（`d=3`、`OP=5`、`PT=4`）**唔套**「開平方分兩支」嗰條規矩，右欄註明「長度取正」——house-style 規範 3 管嘅係解方程 x²=p，唔係幾何量。
- **冇 build script 點改**：18 個單元入面只有 04、05 有 .py，其餘淨係成品 docx。做法＝直接 patch `word/document.xml`，用 `worked_example_table()` 生成表格 XML 換走原段落（`_tbl` 出嘅係純 direct-formatting、冇 style 依賴，所以插得入任何 docx）。patch 腳本喺 scratchpad，**冇留喺 repo**。
- **版控發現**：02/06/08/09/10/11/12 七個單元**成個資料夾從來未進過版控**（開工時 git status 一堆 `??` 就係呢個原因），今次一併補 commit（同 2026-08-08 做法）。
- **8 個 `.docx.bak` 原稿備份 ＋ 02 嘅 `_舊版備份.pdf` 未刪**，等覆核完先決定。
- **NotebookLM 未同步**（今次改嘅 8 份 docx＋pdf 都係更新既有來源，要刪舊加新，按規矩等使用者授權）。

---

**2026-08-12（同日第二輪）：高三丙班「基本初等函數的導數與運算法則」「導數在研究函數中的應用（單調性與極值）」兩單元從舊資料夾搬遷到新資料夾、練習題改寫markup語法、NotebookLM同步。**
- **起因**：同日第一輪列出嘅「待裁決」事項——呢兩個單元教材只存在舊資料夾 `inclusive-derivative-intro_高三數學`，新資料夾 `導數_高三理數學` 冇對應版本。使用者選「搬資料夾＋順便升級呢兩個單元嘅練習」。
- **查證後修正咗自己一個誤判**：原以為練習答案要套 2026-08-05 等號對齊／拆支／∴ 規範，翻查 house-style.md 先發現嗰組規範明文淨係管「範例段」（喺講義入面），練習答案key本身唔屬於管轄範圍。實際要做嘅淨係：①講義兩份其實已經喺 2026-08-10 嗰輪處理過（worked_example_table／D2+D5設計已落地），純搬位置、byte-identical重出；②練習兩份由手寫 `omath(mr(...),sup(...))` 改用 `{}` 標記語法統一風格，並修正輸出路徑寫死舊碟舊資料夾嘅bug（同 `build_practice.py` 嗰個 2026-08-12 第一輪揪出嘅bug同款）。
- **QA**：機械檢查43/44過，唯一FAIL（QB-10題答對應）查證係PyMuPDF文字抽取偽陽性——「1～4題答案共用一行」令佢漏判題號邊界，PDF目視確認全部題目答案齊全正確，已記錄喺對應驗算md（詳見下面踩坑筆記）。
- **NotebookLM**「高三數學導數系列」筆記本同步（先加後刪，加8刪8，source_count維持23）。
- 舊資料夾 `inclusive-derivative-intro_高三數學` 內容已清空並經使用者確認刪除，但資料夾本身俾Google Drive同步鎖住暫時刪唔走（`Device or resource busy`），淨返一個空殼，會自動消失。

---

**2026-08-12（同日第一輪）：①對比 Anthropic 官方 k12-teacher-skills repo，採納4條規則寫入教材生成skill；②整合高三丙班「導數的概念及其意義」單元舊《功課》切線方程內容入新版《講義》＋《練習》＋《工具卡》，NotebookLM同步，退役舊檔。**
- **k12-teacher-skills 對比**（使用者提供 github.com/anthropics/k12-teacher-skills，內含 k12-lesson-planning、k12-lesson-differentiation 兩個技能）：詳細解讀生成原理後對比本工作區既有skill——本工作區嘅 S1-S10數學結構→D1-D14設計選型套路比對方通用8條規則模板更細緻（唔使學）；對方完全冇原生數學符號處理（OMML/LaTeX全欠奉），反而係本工作區強項。採納4條對方有而本工作區冇明文寫低嘅規則，已寫入skill：①練習A鷹架要點名橋接嘅先備技能（teaching-designs.md＋worksheet-generator SKILL.md）②出題前先窮舉結構情況（worksheet-generator＋similar-practice-generator）③練習C/D14揀題用「迷思測試」自我檢查（worksheet-generator）④QB-V5教學設計落地加雙向一致性（math-deliverable-qa＋worksheet-generator）。已存 memory `k12_teacher_skills_adopted_rules.md`。
- **高三丙班「導數的概念及其意義」整合**：查證發現「重疊」其實係教案缺口——教案 09/04、09/08 兩堂寫明要教「導數＝切線斜率」「求切線方程」，但新版《講義》只教到用定義求導數＋線性估計，冇切線方程內容；舊《功課》類型三（曲線的切線方程3題）正好補呢個缺口，唔係單純重複。
  - 《講義》新增「三、導數的幾何意義：求切線方程」節（worked example: f(x)=x²+x → y=3x-1），原「三、順便一提」順延做「四」。
  - 《練習》C 新增第7、8題切線方程（改編自舊功課，7題保留兩步引導＝f(x)=x²-2x+3、8題完全自主＝f(x)=x²-3），原有情境應用/線性估計題號順延做10-12，形成「引導→自主→特例」鷹架遞減梯度。
  - 《工具卡》新增第4張「求切線方程三步卡」，對應教案「發求切線三步卡」。
  - **QA 揪出真bug並修好**：工具卡原有3張卡（連同新加嘅第4張）全部用打字直接輸入 f'(x)/y' 呢類記法，違反鐵律1「數學式必須原生」，`qa_check.py` QB-2 驗到全文 `m:oMath×0`。已全部改用 `{}` 標記語法轉真OMML，重驗過6/6全過。**其他工作區舊教材（尤其手順卡型工具卡）可能有同類地雷，未做全工作區排查。**
  - **修正一個腳本output路徑舊坑**：`build_practice.py` 硬寫輸出路徑指向已停用嘅舊資料夾（`C:\Users\KongChiLok\notebookLM\...inclusive-derivative-intro_高三數學`，同新資料夾唔同碟），已改用 `os.path.dirname(__file__)` 動態抓路徑。
  - NotebookLM「高三數學導數系列」筆記本：25→31→23（加6新版、刪8舊來源—6個舊版本+2個舊《功課》），核數正確。本機舊《功課》3檔（docx/pdf/驗算.md）已刪。
  - **釐清一個中途講錯嘅判斷**：一度以為舊資料夾 `inclusive-derivative-intro_高三數學` 其餘檔案同新資料夾「撞名」係重複，查證後發現錯——「基本初等函數的導數與運算法則」「導數在研究函數中的應用(單調性與極值)」呢兩個單元教材**只存在舊資料夾**，新資料夾冇對應版本，未搬未動，仍在用緊。

---

**2026-08-10：高三丙班理組（導數／積分／空間向量共14份講義）補圖＋書寫規範全面重出、NotebookLM三個筆記本同步、教案進度表更新。**
- **起因**：使用者提供教案《抽離課堂教學教案_高三丙班理組_全學段.html》，查 W1-16 教材充足度與新規格落差——發現格式合規率0%（零圖、零 `worked_example_table`、教學設計未選定）。使用者選「B：補圖＋書寫規範」全面重做，批量授權不中途問。
- **導數主題（5份講義）**：主D5圖文雙軌＋輔D2手順卡＋輔D14錯誤分析對比（首課例外——D14不用在全新概念第一課）；新增3張圖（切線/單調性/凹凸性）；新增《工具卡_導數運算與判定手順卡》（3張D2手順卡：求導四步／單調極值判定／凹凸拐點判定）。
- **積分主題（2份講義）**：沿用原有D6+D11+D2設計不重選；定積分新增2張曲邊梯形/正負區域陷阱圖（`design_svg.py` 的 `parabola_graph()` 新增 `shade` 參數，可畫定積分面積灰底，供全工作區其他單元複用）；不定積分純代數程序不需要補圖。
- **空間向量主題（7份講義）**：主D5圖文雙軌＋輔D7判定定理卡＋輔D2手順卡；**真正實作D5雙軌**（原本「image在上、文字步驟在下」改成 `dual_track_table` 左圖右式逐列對齊，不是裝飾）；新增《工具卡_空間向量判定定理卡》（D7，線面/面面平行垂直共4張判定卡）。
- **全庫 frac/sqrt 書寫規範把關**：發現並修正2組負分數符號位置bug——`練習_空間向量的坐標與坐標運算`第5題、`講義+練習_隱函數求導`共7處 `frac(-2x,2y)` 類寫法，負號應提到分數線外寫成 `-frac(2x,2y)`，docx（`{}`標記）與HTML（`\dfrac`）兩版同步修正。
- **NotebookLM 三個筆記本已同步**（使用者授權刪舊）：導數 15→25（順道補齊之前漏傳的「隱函數求導」「凹凸性拐點」兩課）、積分 12→12、空間向量 28→30。
- **教案進度表已同步更新**：移除「導數/空間向量無工具卡」過時警示（改為已補齊，只剩矩陣仍無）；四則運算、單調極值、凹凸拐點、法向量判定、線面關係等課節「教材」欄加註對應工具卡檔名。
- **已知限制**：本機 Word COM 這節課**全程當機**（多次清殭屍重試仍卡死，非個別檔案問題）——本輪新產出 docx 只做 XML 結構驗證（確認無損毀），未能像以往用 Word 目視核對排版；改以 HTML→PDF（headless Chrome，不受 Word COM 影響）作正式驗收，已逐份目視確認無誤，docx 待使用者之後方便時用 Word 開一次覆核。
- **未處理**：矩陣單元仍無工具卡（教案已標注，非本次範圍）；「功課_導數的概念及其意義(變化率問題)」舊檔與新版《講義》內容重疊未整合，需要使用者裁決；練習題檔案除2處frac修正外未套用書寫規範（範圍限定在講義範例段，練習題內容/答案本身正確）。

---

**2026-08-08（同日第二輪）：高一 03_指數與對數《簡報_L2_分數指數冪》根式／分數指數冪改用原生真分數線＋真根號，soil_kit.js `drawEq()` 加咗兩種新節點型別，另修 3 個幾何排版 bug。**
- **起因**：使用者原意係要修**簡報**跟足新版本（真分數線／真根號），中途一度誤解成要改講義／練習——已還原講義／練習做返原本嘅 `nthroot()`／`frac()` 巢狀寫法（HTML 一直冇問題，`\sqrt[n]{}`／`\frac{}{}` 本身已經係原生）。
- **`soil_kit.js` 新增兩種 `drawEq()` 節點**（供全工作區其他簡報未來複用）：
  - `{ sqrt:..., deg:'n' }`：n 次方根，根指數畫喺 hook 前面（ⁿ√），vinculum 高度自動跟被開方式最高點調整。
  - `{ base:..., exp:... }`：指數，exp 可以係 `{n,d}` 真分數（例如 `a^(2/3)` 入面嘅 2/3 真係一條分數線疊埋，唔係打斜線）；`[...]`（陣列）可以塞入任何一欄砌複合式，如 `(2³)^(2/3)`。
  - `Deck` 多個方法（`problem`／`agenda`／`scaffold4`／`compareCards`／`summary`／`figure` 側欄）加咗 `{eq:[...]}` 支援；新增 `Deck.eqTable()`（表格 cell 可以擺真分數／根號，pptxgenjs 原生 `addTable()` 做唔到）。
- **3 個幾何 bug**（詳細記錄喺下面踩坑筆記）：根號 vinculum 冚唔切指數、分數線同根號分母嘅頂線打交叉、`colLines()` 塞咗分數嘅行高度唔夠疊落下一行。
- **另修一條硬規格**：負分數（含負指數）嘅負號要寫喺分數線**左邊**，唔可以縮埋落分子——三種產出（docx OMML、HTML MathJax、pptx drawEq）逐一核對，docx／pptx 各中招數處已改；已寫入 house-style.md。
- **附帶發現並修好一個共用底層 bug**：`shaded_box()` 誤傳已砌好嘅段落清單會靜默壞掉（內容消失得返亂碼），新增 `shaded_multi_box()`；已 spawn 背景排查任務 `task_63bc8867` 查其他單元有冇中招——**已喺另一個 session 完成，順便修好並重出咗 4.2指數函數 講義／練習、加新工具卡**（詳見下面 2026-08-08 第一輪紀錄）。
- **驗收**：機械檢查 12/12 過；簡報 12 頁全部目視核對，冇再見疊字／打交叉。
- **NotebookLM**：講義／練習 docx＋pdf 已同步（source_count 核實）；**簡報使用者明確表示唔使上傳**。
- **改動檔案唔喺 notebookLM repo 入面**：`soil_kit.js`／`omml_core.py`／house-style.md／跨 session 記憶檔案都喺 `~/.claude/skills/`、`~/.claude/projects/`，唔受呢個 repo 嘅 git 管——今次收工 commit 淨係包到呢個 repo 入面嘅 `build_L2.js`／`exp_draw.js`／`build_topic1.py`／簡報 pptx+pdf。

---

**2026-08-08（同日第一輪）：高一 4.2《指數函數》融合版講義＋練習＋工具卡重出、配合簡報 L3 與教案、驗收通過、NotebookLM 同步。**
- **教學設計**（動筆前問使用者選定）：主 **D5 圖文雙軌**（左圖右式逐列對齊）＋輔 **D7 提示卡**（＝工具卡，首次為此單元新增）＋輔 **D14 錯誤分析對比**（簡報 L3 第9頁三條真實高頻錯法）；鷹架密度＝抽離小班。
- **補齊教案 7 項必教點中舊版缺的 3 項**：y=aˣ 圖像特徵、兩型圖像左右對照、指數圖像卡——舊版零配圖、`mathbb(R)` 未渲染、7 題有 4 題其實是 4.1 內容。
- **新畫 10 張 SVG 座標圖**（`_scripts/figs_L3.py`）：講義大圖兩型對照、D5雙軌四列（定點/值域/遞增/遞減各自不同）、練習側欄三張、工具卡兩型。
- **QA 三輪**（subagent 目視驗證，數學內容三輪皆零錯誤，問題全在排版與圖形）：
  第1輪13項版面問題全改；第2輪修好之餘新增2個回歸（工具卡觸發語更碎、練習提示框公式放大4倍爆版）；
  第3輪全部通過。
- **根因＋新規則（已寫入 house-style.md 與跨 session 記憶）**：CSS 用後代選擇器 `.某某 svg{}` 會連 MathJax 輸出的 `<svg>` 一齊命中（結構是 `<mjx-container><svg>…</svg></mjx-container>`），表面症狀像「MathJax 跑了 display 模式」實際是 CSS 蓋過 inline 公式——**一律改用 `>` 子選擇器**。另一條：漸近線類函數圖（指數/對數/反比例）曲線末端與坐標軸至少留 3px，否則會用圖推翻旁邊「值域 y>0」「碰不到 x 軸」的文字。
- **逐題驗算記錄**：`驗算_4.2指數函數.md`（含圖形數值核對表、教案覆蓋核對表、QA三輪紀錄）。
- **NotebookLM 已同步**（使用者授權刪舊）：高一數學融合班教學資源②，先加6新源、後刪4舊源，source_count 44→46 已核實。
- **教案已更新**：2026/12/01、2026/12/04 兩節「教材」欄補上《工具卡_4.2指數函數》。
- **同時把同資料夾先前未進版控的 4.1/4.3/4.4/對數方程等既有交付檔與 L1-L10 簡報一併補commit**（單一 commit `baf87c0`，已 push）。

---

**2026-08-07：高一 L2《函數的表示方法與分段函數》講義＋練習按新規則重出、驗收通過、NotebookLM 同步。**
使用者出冊前「定案」新規則三點，講義練習內容零改、只換教學設計：
- **教學設計**：主 **D5 圖文雙軌**（y=|x| 逐段「圖上看→算式上寫」）＋輔 **D2 手順卡**（四步驟）＋輔 **D12 自我核對**
- **新規則三點全實裝**：
  1. **圖象法直接出圖**：情境段三顆分散的影印店點圖取代文字描述
  2. **分段函數一律大括號**：新建 `omml_core.cases()` 建構器、`{cases(…)}` 標記語法、 `domain_segments()` 褪除梯度
  3. **言語精簡**：式子寫得清楚嘅中文敘述全刪，圖下只留一行教學重點 caption
- **產出**：講義（4頁 docx/html/pdf）、練習（7頁 docx/html/pdf 含10 題答案）、
  新增圖形組 `svgs_expr_piecewise/`（6 張 svg＋png：5 種圖象）、
  驗算主檔添註「2026-08-07 改版核對」。
- **QA 一輪機械＋目視＋重算**：6 檔全 PASS（修正 QA checker 假警報 QB-15c）、
  10 題逐題驗算，交界值（x=0、x=2、x=10 等）連續性已核實。
- **house-style + inclusive-deck-checklist 同步升版**：新增兩節「分段函數大括號寫法」
  「講圖就要出圖」＋沿革註記；簡報版明確禁止散文條列。
- **記憶檔已建立**：`memory/piecewise_must_use_cases_brace.md` + `draw_the_figure_not_describe_it.md`
  （兩條新規則持久化）。
- **NotebookLM 已同步**：高一數學融合班教學資源 ① 筆記本，加 4 新源+刪 4 舊源+刪重複 PDF，
  source_count 38 已核實。

---

**2026-08-06（同日第二輪）：單元04（分式方程／無理方程／二元二次方程組）全套重寫、覆蓋舊檔。**
使用者指定用 `inclusive-math-worksheet-generator` ＋ `inclusive-soil-teaching-deck` 兩個 skill。
- **教學設計**（動筆前問使用者選定）：主 **D2 手順卡**＋輔 **D12 自我核對**、**D14 錯誤分析對比**；
  三子題等重、一份講義做足。全單元主線＝「三種方程同一念頭（把未知數放出來），
  代價是可能生出假解，所以最後一步永遠是驗根」。
- **產出**：講義（5頁 docx/html/pdf）、練習（8頁，A/B/C 各3題共9題）、
  **工具卡（新增檔案，舊版沒有，4張卡：三類方程四步＋驗根小卡）**、簡報（24頁 pptx/pdf）、
  驗算主檔（13題逐題代回原方程核對）＋簡報驗算檔改寫成頁面對照。
- **QA 兩輪 subagent 驗證**，共修 1 必修＋2 版面必修＋若干建議：
  ① 講義範例三「分兩支」左右支交叉（寫 `x+4=0` 卻接 `x₁=3`）＝教錯，已改 `(x-3)(x+4)`；
  ② 簡報分層任務頁 C 欄文字疊字糊成一團；③ 簡報辨識頁底部提示壓穿卡片框線。複驗全 PASS。
- **NotebookLM 已同步**（使用者授權刪舊）：刪 4 個舊來源、補 7 個新來源，
  source_count 55→58 已核實。
- **教案 HTML 已更新 v4→v5**：單元04 練習題號全部重編（舊版 B3/C6/C7 等已失效）、
  加入**課時提醒列**（使用者特別要求）——原定 3 節極可能不足，乙班建議 4–5 節、丙班 5 節，
  並附「時間緊時的取捨次序」與「不可割項目」。

---

2026-08-06（同日第一輪）：根據教案《抽離課堂教學教案_示範填寫_初三乙丙班》v3標示的教材缺口，
補齊初三單元05（二次函數的圖像與性質）——
① 講義新增 §三平移階梯（3.1上下移／3.2左右移含h正負號陷阱／3.3合成＋平移路徑總結表，
   教學設計 D5圖文雙軌對照＋D11標記對應a/h/k）、§六待定係數法（頂點式／一般式／交點式
   決策表＋3個worked example，乙班只教頂點式、丙班三式都教）；原§一~§四內容原封不動保留；
② 練習新增題3（平移辨識）、題10-11（一般式／交點式待定係數法），全部題號重新編號；
③ 新增共用工具 `design_svg.parabola_graph()`（黑白座標圖，虛線＝原圖／實線＝結果／
   點線＝h陷阱對照，之後其他單元要畫函數圖可直接複用）；
④ 使用者驗收後追加2個分頁修正（平移路徑總結表＋§六標題都逼到同一頁），已重生PDF確認；
⑤ docx→PDF雙軌驗證通過，逐題親自驗算記錄在同資料夾《驗算_二次函數的圖像與性質_補充內容.md》；
⑥ 已同步 NotebookLM「初三數學融合班講義練習(上)」筆記本（刪舊補新4個來源，source_count 55不變）；
⑦ 同步更新教案HTML（`_行政表格\抽離課堂教學教案_示範填寫_初三乙丙班.html`）：
   單元05兩處「⚠️ 待補」教材欄改為實際章節引用，版本 v3→v4。
單元06「圖像法解不等式」（丙班獨有，講義練習答案三處全零覆蓋）今次未處理，教案已保留標示。

---

## ➡️ 下一步

### 每週小測（2026-09-01 新增，等使用者裁決）
W05 樣板已出並驗收，三項等你決定：
1. **檔名要唔要改**：改 `測驗_` 可即時通過 QB-6（白名單內），但「測驗」喺呢個工作區指
   段考卷，會撈亂檔案室分類。維持 `小測_` 就永遠帶住 2 個 FAIL。
   （注意：加白名單都解決唔到 QB-8／QB-10，見踩坑筆記分卷型嗰條。）
2. **未覆蓋配方法同十字交乘型因式分解**（5 題限制下嘅取捨，已寫喺教師卷命題說明）——
   下次小測要唔要補返。
3. **擴到成個學段**：規格卡嘅 ⚠️ 風險——跟丙班進度出卷，丙班全學年多 15 節，約 W9
   之後乙班有機會測到未教內容，擴產前要先定處理方式。

### 🔴 最優先：私隱阻塞（2026-09-01 新增，未推 GitHub）

**`kongchilok/math-materials` 係 PUBLIC repo**（`gh repo view` 實查 `isPrivate:false`）。
以下 3 個檔含個人資料，**已 hold 住冇 commit、冇 push**，等使用者裁決：

| 檔案 | 含乜 | 狀態 |
|---|---|---|
| `_行政表格/課後輔導教學教案_初三數學補救班_全學年.md`（新檔） | 6 個融合生真名 | 未入版控 |
| `_行政表格/抽離課堂教學教案_初三丙班_全學年.md`（v1.2 註記） | 7 個融合生真名 | 已入版控但本次改動未 commit |
| `_行政表格/抽離課堂教學教案_高一戊班_全學年.md`（v1.1 註記） | 一位同事老師真名（該名字**已存在於版控歷史**，見下） | 同上 |

- 🔴 **但同事老師嗰個名已經漏咗**：`git grep HEAD` 查到高一戊班教案嘅**已 commit 版本**
  已經有嗰個名——`git log -S` 定位係 `9972084`／`5611d15`（2026-08-25／26 六班教案產出）帶入，兩個 commit 開工前已經喺 origin，**唔係 2026-09-01 呢次推上去**。**學生名一個都未漏**（檔名同內容都查過）。
  要清同事嗰個名就要改寫 git 歷史（`filter-repo`／squash）＋強制推送，屬破壞性操作，等使用者裁決。
- ✅ **學生真名零外洩**（`git grep` 查過 HEAD）——即係一路未漏過，仲截得住，唔使改寫歷史。
- **三條路（等使用者揀）**：
  1. **改代號**（跟鐵律 6：學號／代號代真名）→ 改完照推，最乾淨。
  2. **repo 轉 private** → 真名可留，但要留意 GDrive 分享設定係另一回事。
  3. **加 `.gitignore` 排除 `_行政表格/`** → 教案永不入版控，但就冇咗版本備份（丙班 v1.2 講明
     「被移除嘅 31 節原文保留喺備份檔」，而嗰個備份檔根本唔存在，全靠 git 先復原得返）。
- ⚠️ 連帶：丙班 v1.2 註記講嘅備份檔 `data/backup_初三丙教案_before_v1.2_20260829.md`
  **全 repo 搵唔到**，`_行政表格/data/` 資料夾根本唔存在。內容仍可由 commit `635d0b8` 復原，
  但個註記係錯嘅，要補檔或改註記。

### 封面（2026-09-01 新增，低優先）
1. **校名要唔要加返**：使用者喺內容題冇剔校名（但揀中嘅預覽圖有）。
   `build_cover.py` 第 27 行 `SCHOOL = None` 改成字串即加返、重出。
2. **`qa_check.py` 的 `GENRE_RE` 冇收「封面」前綴** → 呢 3 份封面跑 QA 會**靜靜咁 skip**
   （就係 2026-08-21「附件_」害缺陷匿咗 4 個月嗰個機制，見踩坑筆記第一條）。
   2026-08-21 對「詞彙卡」裁決過唔加（理由：零落地、profile 未定），封面已有 3 份落地，
   要唔要加係另一個決定——未做，等裁決。
3. **未上傳 NotebookLM**：封面唔算教材內容，本輪冇上傳。


### ~~🔜 新學期開學即做（2026-08-19 登記，卡住等校曆）~~ ✅ 2026-08-25 已完成

**原三類檔案 × 三個年級全部完成**：IEP 第 8-9 點六份（僅改起迄日期，長期目標／服務內容／主要目標一律未動）、
三份教學計劃（初三 v4／高一 v2／高三 v2）、七份教案（含新開嘅高一第二學段）。詳見上面
「⏯️ 上次做到哪」2026-08-25 條目。

- ✅ **高一教案「只得第一學段」**：裁決**新開第二學段一份**（`抽離課堂教學教案_高一甲戊班_第二學段.html`），
  同初三、高三一致做全學段覆蓋。
- ✅ **高二仍不在範圍**：維持 08-19 裁決，`IEP第8-9點_高二數學_26-27全學年_融合班通用草稿.docx`
  無教學計劃、無教案，備用檔案。

---

### ~~待使用者決定（2026-08-21 新增）~~ ✅ 六項已於 2026-08-21 第二輪全部裁決並執行

1. ~~2 份空白範本冒充交付檔~~ ✅ **已刪**。核實：兩檔 md5 同為 `aa9c6e04…`、內容係 house-style
   範本佔位符，全工作區只有呢 2 份中招；而且 `build_inverse.py`／`build_logarithm.py`
   **只出 docx 冇 HTML 輸出**，同單元的 `練習_4.3`／`_4.4` 一樣冇 HTML——即係呢 2 個 HTML
   由頭到尾都唔屬於產線，係孤兒檔。
2. ~~81 份舊 HTML 沒有重複頁尾（QB-15c）~~ ⏸ **使用者裁決不做**（性質同舊簡報回溯）。
   數字修正：交付檔級係 **76 份**（高一 38＋高二 38），初三零缺口。
3. ~~高一公式紙 02／03／04 各 3 頁~~ ✅ **已解決，零內容刪減**：先加 compact 版面壓留白（唔夠），
   再三組拆張成 8 份，全部 ≤2 頁。詳見上面第二輪紀錄。
4. ~~`附件_立體幾何初步公式_高一數學.docx` 命名~~ ✅ **已改名**為 `公式紙_高一數學_05立體幾何初步`，
   並修好因為命名唔對而四個月冇人揭到的三個缺陷（空白頁／冇頁碼／冇「允許攜帶」字樣）。
5. ~~高二 5 個單元零驗算＋概率2 扁平斜線~~ ⏸ **維持 08-19 裁決：高二不在範圍**。
   本輪核實實況比登記時嚴重，日後真要教高二時第一件事就係補呢兩項：
   - 零驗算係 **6 個單元、40 對教材**（第一學段 01/02/03/04＋第二學段 01_數列/02_排列組合與概率）；
     得 05_線性規劃、06_解不等式 有驗算檔。
   - 扁平斜線唔止一檔係 **5 檔**；其中 `練習_概率2_獨立與條件概率` 係 **oMath × 0**——
     成份文件零原生數學式，`1/3`、`1/2` 全部打字硬湊，**屬鐵律 1 違反**。
     已按記憶 `qb2_omath_zero_two_causes` 抽文字核實：內容完好，係真‧冇轉 OMML，唔係塌檔。
6. ~~`GENRE_RE` 補「詞彙卡」~~ ⏸ **使用者裁決不加**（詞彙卡零落地、profile 未定，加咗會把錯規則寫死）。

**順帶清理（同輪）**：刪初三公式紙資料夾 2 個 `.bak`；退役 `公式紙_高一數學_4.1-4.4指數與對數`
（範圍已被 03A/03B 完全涵蓋，並存有派錯風險，且係 2026-07-21 驗算紀錄記載印出 `a \!
eq 1`、
`(0,+infty)` 壞字串那份）。全部搬去 `_archive\退役交付檔\`。

### 待使用者決定（2026-08-17 新增）
1. **A：關鍵詞彙卡「常設產出」全工作區零落地**——`math-visual-scaffold-generator` 2026-08-14
   才把呢個升為常設項，但初三 18 個單元一份都未回溯補做。另外 `math-deliverable-qa` 的
   `GENRE_RE` 未收錄「詞彙卡」前綴，將來就算補做，現時腳本亦唔會自動覆蓋檢查（要順手加）。
   2026-08-18 已出單元01試範樣本（Artifact預覽）供參考，使用者裁決全工作區批量推廣暫不急做。
   **存檔位置（2026-08-19 查明）**：本機／repo **零檔案**——`模板試驗/_verify_詞彙卡` 係空資料夾，
   全 repo 搜唔到任何詞彙卡交付檔。單元 01 試範樣本**只存在 Artifact**（`一元二次方程詞彙卡`，
   私有連結未分享）。將來要落地就係由零開始，跟 skill 檔名慣例 `詞彙卡_<單元名>`。
2. ~~B：14 個單元練習資料夾完全冇 `驗算*.md`~~ ✅ 2026-08-18 已全數清零：17個subagent分頭
   逐題重算，14個單元由零新建、04/05/06三個單元查缺補齊，連同01單元，18個初三單元逾190項
   計算100%驗算，零真數學錯誤。

### 待使用者決定（2026-08-14 第三輪更新 — IEP兩軌設計＋課程指引）
1. ~~`~/.claude/skills/` 無備份風險~~ ✅ 2026-08-14 第四輪已解決：搬到 GDrive `_claude-skills`＋C碟改junction＋起git repo（3 commits）。
2. ~~選項 (c)：法規／校規未補~~ ✅ 2026-08-14 第四輪已補完：29/2020第二十三條第一款、28/2020第七條（IEP法定依據）／第十一十二條（留級規則）、校本校規全部核對完，詳見上面「上次做到哪」第四輪紀錄。
3. **兩軌設計未完整跑過填表流程**：試跑只做到藍圖（步驟0–2.5），未跑步驟5–6（`fill_iep.py` 填官方範本＋OMML PDF目視驗證）——下次有真實個案時要補跑一次全流程。
4. **QB-21／22／23 未用真實 PDF 實跑**：只做過函數層煙霧測試。**注意：現有 59+ 份舊講義全部冇「正規教學目標」嗰行，掃舊資料夾一定要加 `--legacy-ok`**，唔係會成批 FAIL。
5. **D15 同情境分任務未實際出過一份教材**：規格寫好但未落地過，第一次用嗰陣要留意共同材料揀得啱唔啱（材料要天然含多個資訊層次）。
6. ~~**同質分組 vs 一份講義照顧全班**~~ ✅ **2026-08-19 已討論並結案**：裁決**唔開分組產線**，
   固定「一份講義照顧全班」。理由＝2026/2027 學年三級六班、每週 16 節、17 名融合生，成本未能負擔；
   極端落差個案改走補丁階梯。規則已寫入 `teaching-designs.md` §2.1 ＋ `SKILL.md` 步驟 3.5，
   並存記憶 `teaching_load_no_ability_grouping.md`。**唔使再攞出嚟問。**
7. ~~`task_58c935b5`（修兩個 skill 死路徑）~~ ✅ 2026-08-14 第三輪已手動修完（4 個檔 8 處），該背景任務可以 dismiss。

### 待使用者決定（2026-08-14 新增）
1. **練習答案區 39 個位置**：暫不處理（使用者本輪裁決），如未來要做——關鍵決策點係
   要唔要跟代數單元一樣嚴格套到幾何/概率/三角單元、改法要唔要做到表格對齊（工作量大）
   定只求拆行（工作量細）。3 個存疑邊界案例（13.C6／15.C6／17.B5，定理陳述+代入混埋
   一步化簡）亦待判定計唔計。
2. ~~06 單元教案的附帶發現：練習C7/C8 用咗「交點式」「最值公式 c−b²/(4a)」呢兩個
   講義未正式教過嘅工具~~ ✅ 2026-08-18 已修：C7改用單元02韋達定理、C8改用完整配方法
   重新推導，數值答案不變，已重出docx+pdf並更新驗算檔。

### 其餘待辦

-1. **待確認（2026-08-25 新增，會直接改動高三週次）**：
   - **四校聯考確實日期**：校曆標「估計 03/18–21」，撞正高三 W29（矩陣／導數開課週）——向教務確認後要回頭調整
     `高三數學\教學計劃_26-27高三抽離_甲丙班.md` §3、§4 及對應教案。
   - **中六畢業旅行日數**：校曆只寫「10/12 開始」無結束日，現時教學計劃只扣一日，可能低估。
   - **五份缺教學簡報**：初三單元 14–18（第二學段）、高一第二學段三角函數/向量/立體幾何三份、
     高三複習期教材（甲班 57 節、丙班 20 節份量最大）——詳見各教學計劃 §七／§八嘅死線表。

0. **待使用者裁決（2026-08-10 新增，部分已於2026-08-12處理）**：
   - ~~高三丙班「功課_導數的概念及其意義(變化率問題)」舊檔與新版《講義》內容重疊~~ ✅ 2026-08-12 已整合＋退役舊檔
   - ~~「基本初等函數的導數與運算法則」「導數在研究函數中的應用(單調性與極值)」兩個單元只存在舊資料夾~~ ✅ 2026-08-12（同日第二輪）已搬遷到 `導數_高三理數學`＋練習改寫markup語法＋NotebookLM同步
   - **新增待排查（2026-08-12）**：工具卡型交付檔（手順卡類）可能有同類「打字直接輸入 f'(x) 呢類記法、冇用 `{}` 標記」嘅OMML缺失，今次喺高三導數工具卡揪出並修好，其他單元未做全工作區排查。
   - 矩陣單元（高三丙班 W30-39）仍無工具卡，教案已標注「課堂即時手寫」——需要時可補一份正式工具卡替換。
   - Word COM 本機環境唔穩定係**反覆出現**、非一次性事故：2026-08-10 全程當機、2026-08-12同日第一輪一度恢復正常、同日第二輪又再度RPC fail（連轉一個之前岩岩成功嘅檔都失敗，需清殭屍再等幾秒先得）。新產出嘅導數/積分/空間向量 docx（2026-08-10批）仍只做過XML結構驗證，未曾用Word目視覆核，建議之後Word COM穩定嗰陣抽時間補做一次。
1. ~~**待處理**：背景任務 `task_b9592fe6`——修 01 單元練習第11題 LaTeX 缺字~~
   ✅ **2026-08-19 核實：早已修好，非未完成項**。`git log -S` 定位——bug 由 `eed5901`（08-04）引入、
   `c87a931`（08-05）修正，並已跟 house-style 拆 `\pm` 做兩支。PDF 目視＋重新驗算全部正確，
   零殘留裸 `ight`。該 chip 已隨 app 重啟失效，唔會再觸發。
2. ~~**待使用者決定（跨檔案風格問題）**：舊 8 份簡報是否回溯統一斜體／底線~~
   ⏸ **2026-08-19 使用者裁決：暫時不做**，維持現狀。（規則本身已查明——`inclusive-deck-checklist.md`
   §52/60/88 明文「不用斜體／底線／藝術字」；單元04 新簡報已按規則做。將來如要回溯，
   改動範圍屬全工作區級。）
3. ✅ **書寫規範批量套用已結案（2026-08-15）**：18 個初三單元全部完成／判定不適用
   - ✅ 02／05／06／08／09／10／11／12（2026-08-12，13 段）＋ 01／03／04（原來已合規，
     2026-08-15 讀 PDF 逐頁核實）
   - ➖ 07／13／14／15／16／17／18——核實後確認本身唔適用（坐標描述句／連比例展開／
     單步代入計算，套用反而教錯）
   - ⏸ 練習答案區約 39 處同類寫法——使用者裁決不套用（2026-08-14、2026-08-15 兩次確認）
   - ⏸ 01 單元 15 處「寫算式三條規矩」條文（house-style §6 明文禁止印俾學生，見 memory
     `writing_rules_not_for_students.md`）——使用者裁決保留不清（「01 唔使清」）
4. ~~待補：高一公式紙缺 PDF~~ ✅ 2026-08-17 已補齊（`office-pdf-convert` 一次成功，3頁）。
5. ~~待補（2026-08-06 教案缺口分析）：單元06「圖像法解不等式」~~
   ✅ **2026-08-21 核實：假缺口，教材早已齊全**。2026-08-14 第一輪已整套補齊（講義 §三、
   練習第 9～13 題、教師答案、工具卡），PDF 亦已重出、NotebookLM 已同步（58→60）、
   commit `6608e01`。逐題重算五題全對。本輪真正處理的是**教案上 4 處殘留標記**，已全部更新。
6. **待清理（低優先）**：單元04 資料夾的 `_舊版_build_deck.js.bak`（舊簡報建置腳本，
   改名保留未刪），使用者未表態要不要清掉

---

## 🕳️ 踩坑筆記

- **彩色 emoji 會令 PDF 由 274KB 脹到 4.29MB**（2026-09-01 踩坑）：教師卷內文寫咗兩個
  `⚠️`，Word 轉 PDF 時為咗嗰兩個字元嵌入成套彩色 emoji 字型（Segoe UI Emoji），檔案脹
  **15 倍**。改成 `※` 之後即刻跌返正常。順帶：彩色 emoji 黑白列印會變灰塊，本身已違反
  house-style「黑白列印優先」。**教材內文一律唔好用 emoji，用 `※`／`①②③`／`★☆`**
  （後面嗰幾組係實測通過嘅單色字元）。工作筆記同 Markdown 檔用 emoji 冇問題，只係交付檔唔好用。
- **`problem_box()` 的 `trailing_blank` 空段係壓頁數嘅隱藏大戶**（2026-09-01 踩坑）：
  嗰個空段用足 1.5 行距（`line=360`），一段約 24pt；5 個題目框就係約 3.4cm，正正就係
  「差少少就一頁」嗰種情況嘅元兇。修法：`trailing_blank=False` 再自己補一個矮身段
  （`line=120`、`sz=8`）——Word 只係要求連續兩個表格之間有段落，**冇要求嗰段有幾高**。
  配合收窄框內上下內邊距（`tcMar` 80→40 twips），本次令學生卷由 2 頁收回 1 頁，
  **字級／行距／邊界全部冇郁**（嗰三項要問過使用者先可以改，見 §3）。
  壓頁數次序仍然跟 [[table_doc_page_saving_order]]：收內邊距 → 縮圖 → 拆檔。
- **`from omml_docx import *` 攞唔到 `_run`**（2026-09-01 踩坑）：star-import 按 Python
  規則跳過底線開頭嘅名稱。`omml_docx.py` 自己有 `from omml_core import _run, _rpr`，
  但你 star-import 佢之後 call `_run()` 會 `NameError`。要另寫一行
  `from omml_docx import _run`。自訂版面 helper（例如自己砌一條帶底線嘅書寫段）好易撞到。
- **分卷型交付檔（學生卷／教師卷分開兩檔）令 QA 兩項檢查嘅假設失效**（2026-09-01 踩坑）：
  `qa_check.py` 嘅 **QB-8 學生資訊列** 同 **QB-10 題答對應** 都假設「一份檔入面同時有
  題目同答案」。分咗卷之後：教師卷冇學生資訊列（設計如此）會 FAIL QB-8，學生卷冇答案節
  會 FAIL QB-10。**即係話單純將「小測」加入 `GENRE_RE` 白名單解決唔到**，要為分卷型
  另寫規則（例如按 `_學生卷`／`_教師卷` 後綴分流檢查項）。
  ⚠️ 同時提醒返 [[qa_genre_gate_silent_skip]]：檔名唔入 `GENRE_RE` 嗰陣，QB-6 係**直接
  FAIL**（唔係靜靜 skip），但**被 genre 閘住嘅 QB-8／QB-10 就真係完全冇跑過**——
  「QA 得 2 個 FAIL」唔代表其餘全部驗過，要自己數清楚跑咗幾項。

- **`para(spacing=False)` 唔等於「零行距」——佢繼承 Word 內建 Normal 樣式嘅段後 8pt**
  （2026-09-01 做封面時踩到）：`spacing=False` 只係「唔寫 `<w:spacing>`」，而 `build_docx()`
  嘅 styles.xml 冇 `w:pPrDefault`，於是段落跌返 Word 自己嘅 Normal 預設（**段後 8pt、行距 1.08**）。
  平時喺正文睇唔出，但**一放入表格儲存格就會把成格撐高**，而 `bd` 畫嘅下框線係跟儲存格底走——
  結果封面填寫欄嘅線離開標籤足足 **21pt**，睇落好似標籤同線各自為政。
  - **查錯方向差咗兩輪**：先後試過 `vAlign='bottom'`、取消固定 `trHeight`，兩次都零改善，
    因為根本唔係垂直對齊或列高問題。**靠 PyMuPDF 抽 text bbox 同 get_drawings() 量實座標**
    （文字底 365.6 → 線 386.7）先確定係「格底本身太低」，唔係肉眼估。
  - **正解**：喺表格內要精確控制高度，一律用明寫零嘅段落——
    `<w:spacing w:line="240" w:lineRule="auto" w:before="0" w:after="0"/>`。
    修好後 21pt → 9pt（淨低 descender ＋ `_tc` 嘅 tcMar 3.5pt，正常）。
  - **同類**：`blank()` 用 `line=360 auto`，放入儲存格一樣會撐高，唔好攞嚟做「空白格」填充。

- **全形底線字元 `＿＿＿` 印出嚟係虛線，唔係一條線**（2026-09-01）：喺
  微軟正黑體→Word→PDF 呢條管線上，每個 `＿` 之間有肉眼可見嘅斷口。
  要一條連續嘅填寫線就用**真正嘅下框線**（`_tc` 嘅 `bd=('none','none','single','none')`）。
  `student_info_row()` 用 `＿` 係因為佢喺正文一行入面、字細睇唔出，封面放大就現形。

- **撐標題字距用 `_run(track=N)`，唔好用全形空格拼**（2026-09-01）：全形空格喺
  字元之間同字組之間一樣闊，「初　三　數　學」會讀成四個散字，分唔出「初三」（年級）
  同「數學」（科目）。`_rpr()` 本來就有 `track` 參數（單位 1/20 pt），用佢。

- **檔名唔啱命名慣例＝該檔永遠唔會被 QA 檢查，缺陷可以匿足幾個月**
  （2026-08-21 第二輪踩坑）：`附件_立體幾何初步公式_高一數學.docx` 因為前綴「附件_」唔喺
  `qa_check.py` 的 `GENRE_RE`（`講義|練習|功課|複習|測驗|公式紙|簡報|工具卡|整合教材|教學計劃|IEP`）
  之內，`genre_of()` 回傳空字串，**所有公式紙專屬檢查（QB-13a 允許攜帶、QB-13b ≤2 頁、
  QB-4 頁尾頁碼）全部 skip**。結果佢多咗一頁全空白、冇頁碼、連「本頁允許攜帶入場」
  嗰行都冇——由 2026-04 存在到 2026-08-21 冇人揭到（今次係手動逐檔量頁數先揪到）。
  **教訓**：QA 報「全 PASS」之前，先確認**每個交付檔都真係被歸類到某個 genre**；
  改名令佢入返 `GENRE_RE` 之後，QA 即刻自己抬出兩條新 FAIL。

- **`{}` 標記語法寫錯 token 唔會報錯，會靜靜哋印一串斜體字母出去**
  （2026-08-21 第二輪踩坑）：`{Delta=b^2-4ac}` 印成五個斜體字母 D-e-l-t-a（正確係字面 `Δ`）。
  **兩層檢查都攔唔到**——`validate_data.py` 只掃四類地雷（反斜線 LaTeX、HTML 實體、裸 `infty`、
  未用 `fn()` 包的 log/sin/cos），`build_formula_sheet.lint_field()` 只警告「`{}` 外出現疑似數學記號」。
  `{Delta}` 語法合法、round-trip 過關，所以一路靜默到出 PDF。同記憶
  `docx_math_markup_landmines`（`{Delta}` 變五個斜體字母、`{mathbb(R)}` 露 markup）同一類。
  已開 chip `task_378b3b5f` 建議補「未知 token」白名單檢查。

- **「呢條已經喺另一張紙度」要真係去核，唔好靠印象去重**
  （2026-08-21 第二輪踩坑）：打算由公式紙 02 移走「分數指數冪換算」「負指數冪換算」，
  理由係「03 的 4.1 指數運算已經有」。**實際核對發現只涵蓋一半**——03A 有「正分數指數冪」
  `a^(m/n)` 同「負分數指數冪」`a^(−m/n)`，但**成份公式紙都冇整數負指數** `a⁻¹=1/a`、`a⁻ⁿ=1/aⁿ`。
  照原計劃刪落去，呢條規則會由兩張紙同時消失。跨檔去重之前一定要把被刪內容逐條 grep 過新位置。

- **表格後嗰個空段落刪唔得，但可以壓到 2pt**
  （2026-08-21 第二輪踩坑）：OOXML 規定 `<w:body>` 唔可以用 `<w:tbl>` 結尾，Word 開檔會自動補一個
  空段落（強行刪走會觸發「檔案需要修復」）。要消滅佢造成的尾隨空白頁，改為
  `<w:spacing w:line="20" w:lineRule="exact"/>` ＋ `<w:sz w:val="4"/>`（2pt）。
  ⚠️ 但**單靠呢招唔一定夠**——本次表格底部剛好落在內容區下限，仲要同時收窄下邊界
  （`w:bottom` 720→454 twips）先入到 2 頁。後來加返頁尾＋標頭又逼爆，最後靠把六張圖
  統一縮到 85%（`wp:extent` 同 `a:ext` 一齊縮，唔會裁切）先真正解決。

- **表格型文件「放闊主要欄位」唔一定慳到位，隨時倒蝕**
  （2026-08-21 第二輪踩坑）：想靠收窄名稱欄、放闊公式欄令長公式少斷一行，實測公式紙 02
  溢出反而由 423pt 變 458pt——名稱欄收窄之後，「五個常見冪函數的性質」「解一元二次不等式的流程」
  呢類長詞多斷一行，抵銷有餘。同理，**把 N 條短列合併成 1 條長列亦慳唔到高度**（只慳到
  每列的內邊距同框線，文字行數不變）。表格排版要慳位，實際有效的次序係：
  收內邊距 → 縮圖 → 拆檔；「重排欄寬」同「合併列」要實測先好信。

- **PDF 渲染損壞用眼睛掃檔案列表永遠揪不到，要拿 HTML 源檔跟 PDF 對抽字**
  （2026-08-21 踩坑）：`練習_充分條件與必要條件.pdf` 與 `講義_空間推理.pdf` 的**內文**中文
  大量缺字錯字（字型子集嵌入失敗），但檔名、大小、頁數、producer 全部正常，QA 的 QB-2／QB-12
  也不會響——因為抽得到「一些」文字。可靠偵測法：**拿同名 .html 的中文片段（連續 4 字為一個樣本），
  逐一檢查能否在 PDF 抽取文字中找到**；正常檔只有 2–10% 找不到（換行切斷），中招的是 55%／90%。
  全工作區 350+ 對 HTML/PDF 只有 2 份中招。⚠️ 這個比率法有**假陽性**：若 PDF 其實是由 .docx
  出的（producer 是 Word）而同名 .html 內容不同版，比率也會很高——先看 producer 再下結論。
- **docx 與 HTML 是兩條獨立管線，一邊正確不代表另一邊正確**
  （2026-08-21 踩坑）：同一課的 `{}` 標記 docx 與 LaTeX/MathJax HTML 各自建置。今次兩個 bug
  **都只有 docx 中招、HTML/PDF 完全正常**——`{mathbb(R)}` 印成 𝑚𝑎𝑡ℎ𝑏𝑏(𝑅)（`mathbb` 不是 `{}`
  語法的 token），以及三份講義的範例整段變成 `wwwww`。**只看 PDF、只看 NotebookLM、只看列印版，
  這兩類 bug 一輩子發現不了**；驗收要真的打開 .docx 或抽它的 document.xml。
- **`shaded_box()` 收到已砌好的段落會靜默塌成一個 `w`——共用底層早有註解，個別腳本卻沒跟**
  （2026-08-21 踩坑）：`shaded_box([para(...), para(...)], kind='example')` 這種寫法，
  每個 `<w:p …>` 字串被當成 segment，只取到第 2 個字元 `w`，五段就是 `wwwww`。
  正解是 `shaded_multi_box()`。**這個坑 2026-08-08 就在同一個資料夾（03_指數與對數 4.1指數運算）
  發現並寫進 `omml_docx.py` 的 docstring，但 build_topic2/3/4 三個腳本一直沒改**——
  教訓：底層修好＋寫註解不等於呼叫端跟上，發現這類 bug 要**全工作區 grep 同樣的呼叫式**，
  不能只修當下那一個檔。
- **只掃「完全沒有驗算檔的資料夾」會漏掉「有檔但只覆蓋一部分」的**
  （2026-08-21 踩坑）：第一輪用「資料夾內有交付檔但沒有 `驗算*.md`」掃出 9 個缺口，
  但 03／04 兩個資料夾**有**驗算檔、只覆蓋其中幾課，就這樣溜過去了；是後來跑 `qa_check.py`
  的 QB-11（逐題號比對覆蓋率）才揪回來的，實際多出 7 份練習沒驗算。
  教訓：覆蓋率要**逐題號比對**，不要只看檔案存不存在。另外 QB-11 認的題號格式是行首
  `N．`／`N、`（`QNUM_RE = ^\s*(\d{1,2})\s*[．、]`），驗算檔若寫成 `- **1** …` 或 A1/B4/C9
  標籤，就算內容真的覆蓋了也會 FAIL——寫驗算檔時題號要頂行寫 `1．`，或補一段題號對照。
- **查「呢個 bug 修咗未」唔可以只睇 `git show --stat`，要用 `git log -S` 加驗成品**
  （2026-08-19 踩坑）：查 01 單元 LaTeX bug 時，`git show --stat c87a931 | head -30` 只列到 3 個檔、
  冇見練習 html，差啲得出「修正唔喺呢個 commit」嘅錯誤結論——**真正原因係嗰個 commit 嘅 message
  長達 26 行，`head -30` 早就截走咗檔案清單**。改用 `git log -S '<字串>' -- <檔>` 就精確定位到
  引入 commit（`eed5901`）同修正 commit（`c87a931`）。教訓：①`--stat` 配 `head` 會靜默截斷，
  查檔案清單要 `--stat --oneline` 或者索性 `--name-only`；②最終仲係要**驗成品**（render PDF 出 PNG
  目視＋重新驗算），唔好淨係信 git 歷史或者原始碼字串——今次就係靠 PDF 目視先真正確認渲染正常。
- **獨立工具（`worksheet-tools` 呢類）唔喺 house-style.md 管轄範圍內，但唔代表唔使跟**
  （2026-08-17 踩坑）：`inclusive-math-worksheet-generator` 嘅書寫規範定案（拆支寫或/判斷
  不佔算式行/分數疊字等）淨係寫入咗嗰個 skill 嘅 house-style.md，路由表以外嘅獨立工具
  （`worksheet-tools`）冇人主動同步。今次先發現呢個工具建於規範定案之前，10年份、3380行
  教師版詳解全部未跟過——一直冇壞、亦冇人揪到，直到今次專門去查先發現。教訓：以後工作區
  新增／既有嘅獨立工具（唔經過 skill 管道嘅），都要記得定期同 house-style 對齊一次，唔好
  以為只有 `inclusive-math-worksheet-generator` 出嘅嘢先要跟呢套規範。
- **呢部機開唔到 symlink，改用 Junction 一樣達到效果**（2026-08-14 第四輪踩坑）：
  `New-Item -ItemType SymbolicLink` 拋 "Administrator privilege required"（冇開 Developer Mode、
  冇 admin session）。改試 `New-Item -ItemType Junction` 唔使 admin 權限就成功，跨 C:→G: 磁碟
  一樣work，對「目錄透明重定向」呢個用途效果完全一樣。之後要幫任何資料夾做「本機捷徑指去
  GDrive」都可以直接用 junction，唔使諗symlink。
- **`*.gov.mo` DNS 打唔通唔止係本機網絡問題，Claude 雲端 WebFetch 基建本身都打唔通**
  （2026-08-14 第四輪踩坑）：之前筆記以為 `bo.io.gov.mo` 攞唔到係使用者本機網絡限制，今次用
  WebFetch 直接試都係 `getaddrinfo ENOTFOUND`——即係雲端側都解析唔到嗰個域名。教訓：呢類
  官方域名打唔通，唔好假設「換一部機/換網絡就得」，要諗第二來源（使用者本機下載後親自提供
  PDF），或者搵鏡射域名（但都可能撞 403）。
- **skill 子資料夾自己有獨立 `.git`（nested repo）會令父層 `git add -A` 卡住**（2026-08-14
  第四輪踩坑）：起 `_claude-skills` git repo 嗰陣，`rdq` skill 資料夾本身已經有自己嘅
  `.git`（獨立 GitHub 遠端），父層 `git add -A` 會出 `git rm --cached rdq` 嘅 embedded-repo
  警告。確認咗個獨立遠端歷史仍在（`mathruffian-dot/rdq-skill`）之後，直接刪走
  `rdq/.git` 折埋做普通檔案，避免變成意外嘅 git submodule 參照。
- **筆記庫根目錄係 `AI learing\2ndBrain\每日筆記\`，唔係 `2ndBrain\每日筆記\`**（2026-08-14 第三輪踩坑）：
  4 個 skill 檔共 8 處引用寫漏咗中間 `AI learing` 嗰層。`G:\我的雲端硬碟\2ndBrain\每日筆記\`
  **根本唔存在**，但 skill 只係寫住「一定要讀」冇任何 fallback——會**靜默讀唔到**主依據，
  然後照樣出教材。已全部修正並喺 CLAUDE.md 路由表加警告。教訓：skill 寫死絕對路徑嘅時候，
  最好順手 `ls` 一次確認，尤其係跨 vault 引用。
- **由 L 級逐年推年級係錯嘅（唔止「冇法源」，係方向相反）**（2026-08-14 第三輪踩坑）：
  上輪寫嘅「L16≈初一、L17≈初二、L18≈初三」係照**人教版**序列推。澳門實際序列（《澳門初中
  數學大綱》1999 表6）唔同——因式分解／分式／多項式喺**初一**、畢氏定理同頻數分佈喺**初二**，
  同推出嚟嘅結果相反。官方只有**帶狀**對應（初中＝L16–L18）。連帶：**落差要以帶狀下限起算**
  （初三生喺 L12 → 落差 4 級，唔係 6 級）。教訓：喺澳門情境唔可以攞內地教科書序列做預設。
- **NotebookLM `source_delete` 批量刪除跨筆記本會直接 fail**（2026-08-14 踩坑）：
  一次過傳 11 個 `source_ids`（橫跨3個唔同筆記本）用 `source_delete(source_ids=[...])`，
  回應 `{"status":"error","error":"Failed to delete sources."}`，重試一次都係同樣錯。
  改用 `source_delete(source_id=...)` 單個逐一刪就全部成功。日後跨筆記本嘅刪除操作，
  直接逐個刪，唔好嘗試 bulk。
- **Word COM PDF 匯出間歇性 RPC 失敗，唔好一次失敗就斷定「引擎壞咗」**（2026-08-14
  踩坑）：背景 agent 幫 06 單元轉 PDF 時，主方法＋對照組（未改動嘅檔）＋唔同路徑＋
  `ExportAsFixedFormat` 全部失敗，甚至一個得個「hello」嘅空白文件都存唔到PDF，
  診斷懷疑係預設印表機（網絡 Konica Minolta）離線累事，建議使用者手動改預設印表機。
  但另一個背景 agent（同一時段，修工具卡）嘅 PDF 轉換都撞過 RPC 失敗，一樣清殭屍
  retry 之後就成功；及後我自己重試 06 單元嗰 3 個檔案，**一次過全部成功，冇再撞任何
  問題**——證實純粹係間歇性 RPC flakiness（同已知嘅 [[Word COM 環境級當機]] 同一
  類），唔係「PDF匯出引擎整個壞咗」咁嚴重。下次撞到類似情況，先用標準梯級（清殭屍
  retry一次）試多一次先，唔好因為一個 agent 嘅詳盡診斷就跳去改系統設定嘅結論。
- **git `.git/index.lock` 卡住時，先確認冇 git process 跑緊先刪**（2026-08-14 踩坑）：
  `git add` 報 `Unable to create '.git/index.lock': File exists`，用
  `tasklist //FI "IMAGENAME eq git.exe"` 確認冇任何 git 相關 process 跑緊、
  lock 檔案時間戳係前一日殘留（0 bytes），確認係 stale lock 先刪除，唔好見到 lock
  就話有其他 process 用緊而卡住唔敢刪，亦唔好未查證就當係 stale 亂刪。
- **docx 手插分頁一定要用 `<w:pageBreakBefore/>`，唔可以用 `<w:br w:type="page"/>`**
  （2026-08-12 第三輪踩坑）：後者出嘅 docx **XML 完全 well-formed**、ElementTree
  解析得到、結構驗證全過，但 Word COM `SaveAs2` 會拋
  `com_error (-2147023170) 遠端程序呼叫失敗`，轉唔到 PDF。同一份檔剷走分頁符即刻
  轉得返，換 `pageBreakBefore` 亦正常。正確寫法：
  `<w:p><w:pPr><w:pageBreakBefore/><w:spacing w:after="0" w:line="20" w:lineRule="exact"/></w:pPr></w:p>`
  （`w:line="20"` 係壓走呢個空段落嘅可見高度）。已存 memory
  `docx_pagebreak_kills_word_saveas.md`。
- **SaveAs2 失敗唔好即刻當「Word 又當機」——先用未改動嘅檔做對照組**
  （2026-08-12 第三輪）：呢次一開始以為又係 [[Word COM 環境級當機]] 嗰種
  （2026-08-10 嗰次），差啲就跟舊路線改道去 HTML→Chrome。實際做法：拎同資料夾一份
  **未改動過**嘅 docx 轉一次——對照組轉得成＝Word 正常，問題出喺自己改嘅 XML；
  對照組都轉唔到＝先至係環境問題。呢一步幾秒鐘，慳返成輪誤診。
- **patch 既有 docx 嘅錨點必須落喺單一 `<w:t>` run 入面**（2026-08-12 第三輪）：
  用「傾印出嚟嘅段落文字」揀錨點會中伏——例如段落顯示「…16＞0，兩個交點。解 …」，
  但「兩個交點。解」其實跨咗兩個 run（中間夾住 OMML），`xml.find()` 搵唔到。
  做法：先 `re.finditer(r'<w:t[^>]*>([^<]*)</w:t>', xml)` 列出真實 run 邊界再揀錨點。
- **`worked_example_table` 嘅 `col_frac` 調闊左式1 記住睇埋左式2**（2026-08-12 第三輪）：
  house-style 建議嘅長左式配置 `(0.380,0.050,0.140,0.060,0.110,0.050)` 令
  `(x−3)(x+1)` 一行放得落，但同一張表嘅 `or_row` 右支 `x+1` 反而俾 0.110 逼到斷兩行。
  兩邊都要夠位先得，實測 `(0.350,0.050,0.100,0.055,0.165,0.050)` OK。
  呼應 house-style 原文嗰句：改欄寬之後**一定要重轉 PDF 目視**，唔好淨係計數。
- **範例段套規範會令頁數升，要順手加分頁**（2026-08-12 第三輪）：一行多等號拆成
  逐行對齊之後，一個範例由一句話變 5～11 列表。8 份入面有 4 份（08/09/11/12）
  第一張表**跨頁**，次頁淨返重印表頭，學生睇唔出喺計邊題。判斷方法：
  數 PDF 入面「這一步在做什麼」出現次數 vs docx 入面七欄表數目，多咗就係跨頁。
- **pptxgenjs 沒有原生分數／MathJax**：鐵律1（數學式必須原生）在 pptx 裡要自己畫。
  已建立 `frac(s,cx,cy,num,den,size,color)`＋`fracRow()` 疊字慣例（沿用 `簡報_概率初步12`
  既有寫法），之後開新簡報遇到分數直接複用，不要用「/」文字硬湊。
- **pptxgenjs `addText` 預設有內邊距**：分子分母框不設 `margin:0`＋`wrap:false`，
  長分子（如「3 ± 7」）會在框內換行、字元散開——疊字分數框務必兩者都設。
- **疊字分數框高度不能只算字高**：`hh` 要留 `fontSize/72*1.35` 左右，太緊會令文字
  溢出、蓋到鄰行內容。
- **等號對齊只能靠表格欄位**：段落左對齊做不到——左式一長，等號就跟著右移。算式欄要切成
  `左式｜＝｜右式｜或｜左式｜＝｜右式` 七小欄，小欄之間不畫線。
- **docx 等號格不能加前導空白 run**：那個 run 是為了防 Word 把純公式段落自動置中，
  但 12pt 中文空白會撐高行盒，儲存格垂直置中後等號會比左右低一截。等號格要 `pad=False`。
- **docx 單支列的右式要 gridSpan 吃掉後面 5 欄**，否則 `Δ=(−3)²−4(2)(−5)` 會被迫斷行。
- **HTML 長表要開 `.long`**：`.d-tbl` 預設 `break-inside:avoid`，一頁放不下就整表推到下一頁，
  上一頁留一大截空白。加 `.long`＋`<thead>` 讓它跨頁、表頭次頁重印。
- **LaTeX 一定要用 raw string**：`'\right'` 的 `\r` 會變回車字元，MathJax 靜默渲染失敗，
  PDF 印出 `ight)^2`。同類：`\times`/`\theta`/`\text`→TAB、`\frac`→FF、`\alpha`→BEL、`\vec`→VT。
- **Bash heredoc 會再吃一層反斜線**：改這類字串請用 Write 寫成 .py 再跑。
- **docx 表格無法強制「整個唔跨頁」**：只有逐列 `cantSplit`，多列表格照樣會在列與列之間被
  切開跨頁。實測有效做法：把表格前的標題用 `heading(..., page_break_before=True)` 逼佢喺
  新頁頂開始，內容夠短（3~4列）自然就唔會再跨頁——比硬啃 docx 的表格機制簡單好多。
- **拋物線 SVG 點標籤同 x 軸刻度數字迎面撞**：貼近 x 軸嘅點座標標籤（例如 (3,0)）如果
  `dy` 唔夠大會同刻度數字行高度重疊，`dy` 至少要 28px 先避得開（`design_svg.parabola_graph()`
  已修正，之後用呢個工具畫圖記得留意這個間距）。
- **舊版（無新書寫規範前）內容唔好回頭套新規範，除非使用者batch核准**：單元05呢次淨係喺
  舊講義前後插新章節，原有§一~§四（含原有2張嵌入圖）逐字節重現、完全唔改，新增章節先用
  worked_example_table等新規範——批量套用17個單元仍然係獨立待裁決事項（見上）。
- **`{}` 標記語法嘅括號必須成對，唔可以攔腰切開嚟做填空**：寫 `{4=x(} ＿＿＿ {)}` 會直接拋
  `MathParseError: 括號不成對`。半成品鷹架嘅正確做法係**「式子畀完整、只留決策點空格」**——
  例如步驟2、3 直接印出完整算式示範，學生填嘅係「最簡公分母係乜」同「兩支各自等於幾」。
  咁樣其實更貼合 house-style 講嘅「填關鍵空格」，切斷式子反而令學生睇唔明個式子。
  同類地雷：`{=0}`／`{=-2!=0}` 呢啲等號開頭嘅殘式，改成完整式 `{x-1=0}`／`{-1-1=-2}`。
- **因式分解嘅左右次序要同 x₁／x₂ 對得上**：寫 `(x+4)(x-3)=0` 卻接 `x₁=3 或 x₂=-4`，
  左欄由上而下讀就變成「x+4=0 ⇒ x=3」，**係教錯**（QA 驗出）。答案本身啱都無用，
  範例段嘅視覺對應關係本身就係教學內容。落 `or_row()` 前先確認左支對左解。
- **pptxgenjs 分層任務頁：多行文字會疊字**——每項固定步進（`ty += 0.72`）遇到 3 行題目
  就會疊落下一項度，糊成一團。正解係**「題目區固定高＋提示區固定起點」分區**，
  唔好畀題目長度推走後面嘅元素。同理，卡片列表嘅高度＋步進要留夠底部提示條嘅位
  （實測 1.12 高 ＋1.25 步進，第 4 張就會壓穿底部提示）。
- **分段函數一律大括號、禁散文條列**（2026-08-07 鐵律定案）：「當 x≥0 時，y=x；當 x<0 時，y=−x」
  那種寫法係學生舊教法、自己教材唔能套用。**講義、練習、簡報一律** 用
  docx(`omml_core.cases()` / `{cases(…)}`)+HTML(`\begin{cases}`)+pptx(drawEq自己畫)。
  分隔逗號必須半形(,)——全形「，」在 Cambria Math 會縮成一顆小點（教錯的坑已踩過）。
  Why: 2026-08-07 使用者用課本截圖退稿，原話「練習必須根據圖中的表示方法」寫。
  學生日後考卷就係要這樣寫，教材自己亂寫等於沒教；輔助鷹架是額外的，不是替代品。
- **講圖就要出圖、唔准文字覆述冇畫嘅圖**（2026-08-07 鐵律定案）：情境「圖象法」段落
  淨係寫「把 (1,0.5)(2,1)… 這些點描在坐標平面上……」=用文字覆述一張未出現嘅圖。
  凡涉及圖象、圖象法、平移、分段定義域——一律用 `design_svg.parabola_graph()`／
  `domain_segments()` 直接出圖，圖下只留一行 caption 講教學重點，描述性文字全刪。
  Why: 2026-08-07 使用者指示「直接生成圖像，刪去不必要的文字描述」。
- **npm install 喺 GDrive 路徑會裝壞**：`ERR_INVALID_PACKAGE_CONFIG`（package.json 讀唔到，
  疑似同步問題）。唔使死磕——設 `$env:NODE_PATH` 指向已驗證可用嘅 node_modules
  （例：`簡報_一元二次方程01-03\node_modules`）就跑得，唔使每個資料夾各裝一份。
- **簡報硬規則喺另一份檢核表**：`inclusive-deck-checklist.md` §52/60/88 明文禁斜體／底線／
  藝術字，house-style.md 冇寫呢條。做簡報前記得讀嗰份，唔好淨係睇 house-style。
- **CSS 揀 SVG 一律用 `>` 子選擇器，唔准用後代選擇器**（2026-08-08 踩坑）：MathJax 輸出係
  `<mjx-container><svg>…</svg></mjx-container>`，`.某某 svg{}` 呢種後代選擇器會連公式一齊命中。
  實測生出兩個睇落無關嘅回歸：`.toolcards svg{display:block}` 令觸發語被切碎、
  `.aside-side svg{width:100%}` 令提示框公式放大4倍爆版——兩次都會被誤診成
  「MathJax 跑咗 display 模式」去查錯方向，其實公式一直係 inline，係 CSS 蓋過佢。
  改用 `.toolcards td > svg`／`.aside-side > svg`＋保險條 `mjx-container > svg{width:auto;
  height:auto;max-width:none;display:inline;margin:0}` 一次解決。已寫入 house-style.md
  同 memory/`css_svg_selector_hits_mathjax.md`。
- **漸近線類函數圖唔可以推翻旁邊嘅字**（2026-08-08 踩坑）：指數／對數／反比例呢類圖，
  曲線末端、標示點如果貼住坐標軸，會用圖推翻自己教嘅內容（右欄寫「值域 y>0」
  「碰唔到 x 軸」，左圖曲線卻同軸箭頭融成一嚿黑）。`design_svg.parabola_graph()` 嘅
  point dict 加咗可選 `r` 參數，坐喺軸上嘅定點（如 (0,1)）用 `r=3.2` 免遮軸刻度數字
  （預設 4.6 連白邊會蓋住）；另外收窄 `xrange`／壓低 `yrange` 令曲線末端同箭頭留返
  至少 3px。已寫入 memory/`graph_must_not_contradict_text.md`。
- **`shaded_box(segments,...)` 唔支援傳一份已砌好嘅段落清單**（2026-08-08 踩坑）：呢個
  函數本身淨係接受**一段**扁平 segment list（`[('t',...), eq(...)]`），如果錯誤咁傳
  `[para(...), para(...), para(...)]`（一份已經砌好嘅段落清單），`para()` 內部會將每個
  `<w:p>...` 字串當成 `('m', omml)` 二元組拆開、攞第二個字元（永遠係 `'w'`）——成個框印
  出嚟得返幾個「w」字，內容全部消失，Word／PDF 都唔會報錯，肉眼睇 PDF 先發現。修法：
  多段內容改用新函數 `shaded_multi_box(paragraphs,...)`（跟 `problem_box()` 同一個呼叫
  方式）。呢個 bug 好可能已經喺其他舊教材中招——已 spawn 背景排查任務排查（見上）。
- **`drawEq()` 前置上標（leading superscript）唔可以用空 base 嘅 `sup(mr(''),...)` 砌**
  （2026-08-08 踩坑，docx OMML 側）：空 base 嘅 `m:sSup` 喺 Word 度會令上標浮埋一邊、
  同後面內容中間留一大截空隙。要用 `m:sPre`（Word 原生「前置上下標」構件，isotope
  記法 ¹⁴C 嗰種）先夠貼服。（呢個技術本身而家冇喺 codebase 用緊，因為原本諗住畀 docx
  用嘅「扁平根式」寫法已經還原返做巢狀 `nthroot()`——但呢條坑本身仍然成立，日後如果
  真係要喺 OMML 度砌前置上標，記得用 `m:sPre` 唔好用空 base `sSup`。）
- **`drawEq()` 根號 vinculum 高度唔可以固定，要跟被開方式最高點自動調**（2026-08-08
  踩坑，pptx 側）：`ⁿ√(aᵐ)` 嗰個指數 `m` 會凸出普通文字嘅 cap-height，如果 vinculum
  頂線淨係用固定 `em*0.60`，條線會穿過個指數、冚唔切。修法：`soil_kit.js` 加咗
  `_eqTopExtent(node)` 遞歸計算 node 最高點（exp／frac／sqrt／seq 各自遞歸），vinculum
  高度改用 `Math.max(em*0.60, _eqTopExtent(inner))`。
- **`drawEq()` 分數嘅分母如果係根號，分數線同根號自己嘅頂線會打交叉**（2026-08-08
  踩坑，pptx 側）：`1/³√(a⁵)` 呢種寫法，分母嘅 `³√` 自己有條頂線，如果分母落得唔夠
  低就會同上面嗰條分數線黐埋一齊、視覺上似打交叉。修法：分母嘅垂直位移改用
  `Math.max(0.68em, _eqTopExtent(分母)+0.05)`，唔淨係固定 0.68em。
- **`colLines()` 塞咗分數／根號嘅行，行高唔可以用固定字級計**（2026-08-08 踩坑，pptx
  側）：一行入面有真分數（例如 `1/aⁿ = a⁻ⁿ`），分母會向下伸，如果行高淨係跟字級計，
  下一行文字會疊埋條分母度。修法：新增 `_eqBottomExtent(node)`（`_eqTopExtent` 嘅鏡像，
  計 node 個底伸幾低）同 `eqExtent(parts, fontSize)`（module-level，`colLines()` 可以
  用嚟計每行實際需要嘅高度：`Math.max(預設行高, top+bottom+0.06)`）。
- **負分數（含負指數）嘅負號要寫喺分數線左邊，唔可以縮埋落分子入面**（2026-08-08
  使用者對住簡報表格截圖親自指出）：`a^(-m/n)` 要顯示成「a 嘅指數＝負號 + 一條 m/n 嘅
  分數線」，唔係「a 嘅指數＝一條 (-m)/n 嘅分數線」——兩者數值相等，但顯示規格唔一樣。
  HTML／MathJax 寫 `-\frac{m}{n}`（減號喺 `\frac` 前面）；docx／OMML 寫
  `mr('-') + frac(mr('m'), mr('n'))`（兩個並排嘅 OMML 片段）；pptx／`drawEq()` 寫
  `{ base:'a', exp:['−', {n:'m',d:'n'}] }`（陣列將負號同分數並排放入 exp 欄）。三者
  嘅低層 builder（`frac()`／`\frac`／`{n,d}`）本身都唔會自動搬個負號出嚟，call site
  要自己記得呢條規矩。已寫入 house-style.md。
- **負分數規矩嘅適用範圍**（2026-08-10 補充釐清）：規矩管嘅係「成條分數本身係負」而分子淨係
  一個被負號直接套住嘅單一項（`frac(-2x,2y)`、`frac(-x,y)`、`frac(-3,4)` 呢類），要改
  `-frac(2x,2y)`。但代入後嘅原始比值展示（例如 `frac(-1,-1)=1` 咁樣展示兩個分量都係負數、
  相除結果係正）**唔算違規**——嗰種係展示代入過程、唔係「呢個分數本身係負」，唔使搬負號。
  判斷關鍵：分子／分母入面**任何一個**唔係單一被負嘅項（例如減法式 `x_2-x_1`），都唔屬於
  呢條規矩管嘅範圍。
- **Word COM 呢節課全程當機（環境級、非個別檔案問題）**（2026-08-10 踩坑）：連續三次
  唔同檔案（有圖、無圖、細細張工具卡）都喺 `Documents.Open`／`SaveAs2` 卡死，清殭屍
  （`Stop-Process WINWORD -Force`）＋降級用 `DisplayAlerts=0`／唯讀開都試過，依然卡。
  同時用 `zipfile`＋`xml.etree.ElementTree` 驗證 `word/document.xml` 完整、可解析，證實
  唔係產出嘅 docx 本身壞——係本機 Word 應用層出事。標準escalation（CLAUDE.md §3）用盡
  兩輪後，改道走「結構驗證（XML parse）＋HTML→PDF 目視」完成驗收，冇再嘗試第三次。
  下次再撞到類似情況，可以更快跳去呢條後備路線，唔使逐個檔案重試。
- **`dual_track_table()` 放圖一定要同 `build_docx()` 共用同一個 `MediaRegistry()` 實例**
  （2026-08-10 踩坑）：`dual_track_table(pairs, media=...)` 會喺呼叫嗰一刻即刻
  `expand_image()` 註冊圖片；如果之後 `build_docx(P, path, media=None)` 冇拎返同一個
  `media` 變數傳入，佢會自己起一個新嘅空 registry，圖片就會喺 docx 入面斷link。要喺
  script 開頭起一次 `media = MediaRegistry()`，`dual_track_table(..., media=media)` 同
  `build_docx(..., media=media)` 兩處都用返同一個變數。
- **Bash 雙引號字串入面 `\$變數名` 唔會展開**（2026-08-10 踩坑）：起 for 迴圈砌 Windows
  路徑用 `"$DIR\$name.html"`，bash 見到 `\$` 會當成「轉義出一個字面上嘅 `$`」，令
  `$name` 唔展開、原字咁樣印出嚟。改用正斜線 `"$DIR/$name.html"`（Windows PowerShell
  接受正斜線路徑）就冇事，或者將反斜線同 `$變數` 中間隔開唔好直接黐埋。
- **PyMuPDF 抽字對「多題答案共一行」嘅QB-10會偽陽性FAIL**（2026-08-12 踩坑）：機械檢查
  QB-10 靠 regex `^\s*(\d{1,2})\s*[．、]`（一定要行首）核對題號同答案是否對得上。若教師
  答案key將幾題短答案並排寫喺**同一行**（例如 `1．A（…）　2．A（…）　3．…`），PyMuPDF
  由PDF抽番出嚟嘅文字係咪會喺每個`<m:oMath>`之間插入換行**唔穩定**——有啲組合啱啱好
  行首匹配到、有啲又匹配唔到（實測：兩題共一行嘅concavity練習啱啱好兩題都撞中換行、
  四題共一行嘅rules練習就只撞中第一題），純屬文字抽取層面現象，PDF目視完全正常、內容
  冇缺漏。搵到呢個FAIL時**先用PyMuPDF直接抽字＋render PNG目視核對**，唔好以為真係漏答案；
  如果想徹底避開呢個偽陽性，答案key改成**每題獨立一行**就會穩定行首匹配（但當時Word COM
  唔穩定，改完轉唔到PDF，最後揀返保留原本已驗證嘅共行版本，唔淨係為咗過機械檢查而冒險）。
- **Word COM 唔穩定係反覆出現嘅環境問題，唔係一次性事故**（2026-08-12 踩坑）：2026-08-10
  全程當機過一次，2026-08-12同一日入面都撞到——第一輪重出高三導數工具卡順利，第二輪
  轉一個之前岩岩先成功嘅檔都RPC fail（`SaveAs2`拋`-2147023170 遠端程序呼叫失敗`），連
  跨檔測試（轉完全唔同嘅另一份docx）都同樣失敗，證實係環境級、唔關個別檔案事。標準
  escalation（CLAUDE.md §4：清殭屍retry一次）用盡後應該即刻改道，唔好死等或者為咗追一個
  純排版微調（例如拆多幾行答案）而反覆重試——已驗證好嘅docx/pdf若冇實質內容變更，寧願
  revert返嗰個排版微調、保留已驗證版本，都好過為追求「機械檢查滿分」冒風險去重跑COM。

---

## 📊 快速參考

### 路由表（動手前先查）
| 使用者說… | 用什麼 |
|---|---|
| 出相似題 | skill `similar-practice-generator` |
| 融合班講義／練習 | skill `inclusive-math-worksheet-generator` |
| 公式紙 | skill `jh-math-formula-sheet` |
| IEP 第 8／9 點 | skill `macau-iep-math-goals` |
| 驗收 | skill `math-deliverable-qa` |
| 轉 PDF | skill `office-pdf-convert` |
| 上傳 NotebookLM | skill `notebooklm-sync` |

### 檔案收件匣
📥 `新任務/` ← 所有原材料放這裡

---

*最後更新：2026-08-14（同日第二輪：IEP兩軌設計實作完成，重寫macau-iep-math-goals skill；第一輪：收尾書寫規範改版收貨、5個工具卡OMML修復、補06單元圖像法解不等式教材、NotebookLM三個筆記本同步；已commit push `6608e01`）*
